# Little Stargazers — Project Handoff & Launch Plan

**Written:** 2026-08-26 · **Last updated:** 2026-09-02 · **Repo:** `vedicastro-alt/AstroEducation` · **Production branch:** `claude/vedic-horoscope-learning-site-fb6fta` — everything through §29 below is now merged and live on it.

> **New agent starting now: read §31 first** if the task touches UI/visual design — a full redesign is sitting on `claude/astro-education-ui-changes-vvbela`, **not yet on production**. Otherwise read §29, then §28, then §27. §21-§30 are all **live on production**. §18-20 are settled/background. `claude/astro-education-ui-changes-vvbela` is the only other branch with real work on it right now, and it is branch-only — see §31 before assuming production still looks the way §2-§17 describe it.

> ⚠️ `claude/vedic-horoscope-learning-site-fb6fta` is this repo's default/HEAD branch — Vercel deploys straight from it, and there's no staging step, so pushing there is shipping to production immediately. Two large reference PDFs were also briefly pushed to production by accident and have since been removed cleanly (see §21) — if you see other stray branches with PDFs in `src/lib/education/` (e.g. `claude/handoff-context-review-8pf8w6`), those are source material for §21's work, not app code, and shouldn't be merged.

> ⚠️ **There is only one Supabase database, shared across Production, Preview, and Development** — confirmed 2 Sep 2026 (a single `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` pair in Vercel, scoped to "All Environments," unlike Stripe which has genuinely separate Sandbox/Live modes and secrets). This has two real consequences worth internalizing before doing anything else: (1) a database migration only ever needs to be run **once**, regardless of which environment you're testing from — there is no separate "sandbox database" to keep in sync; (2) **any report/voucher/purchase created while testing against Preview or Development writes a real row into the production database.** Several test reports and at least one test gift-voucher purchase from this session's own debugging are sitting in the live `reports`/`gift_vouchers` tables right now (no real money moved — Stripe itself was correctly in Sandbox/test mode throughout, `livemode: false` — just non-customer rows mixed into real data). Worth a quick pass in the Supabase Table Editor to identify and delete obvious test rows (test emails, placeholder child names) before this matters for real analytics or support lookups — not done in this session, flagging for whoever picks this up next.

This document exists so a new agent (or the founder) can pick this project up cold, with full context on what's built, why it's built that way, what's been tested, and exactly what's left before launch. It's a handoff, not marketing copy — it says where things are weak as plainly as where they're strong.

---

## 1. What this is

**Little Stargazers** generates a gentle, encouraging Vedic-astrology-based "learning pathway" reading for parents about their child's educational strengths and growth areas. Free preview, two paid one-time tiers. The founder's stated goal is a **genuine passive-income side business** — not a venture-scale play, not a labor-intensive consultation service.

Non-negotiable product stance, established early and held throughout: **warm and emotionally resonant marketing is fine; fabricated reviews, fake urgency/scarcity, and fear-based framing are not.** This isn't a vague ethics preference — it's grounded in Australian Consumer Law (misleading/deceptive conduct, s18; the new unfair-trading-practices regime targeting fake urgency cues specifically), since the business is Australian-owned and -hosted. See §7.

---

## 2. Current status

**The product is feature-complete as a prototype and has been through one real round of user-critique QA with fixes applied.** It has *not* been launched — no real traffic, no content/SEO presence, no email list. Payment (Stripe) was tested end-to-end by the founder and confirmed working before the QA round in this document; the QA round did not touch payment logic. **Update 27 Aug 2026: Stripe is now fully live.** The account cleared manual review, the founder switched `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` in Vercel to live values, registered a live webhook endpoint, and verified the whole path with a real purchase (report unlocked correctly, webhook delivery showed `200`). No hold on traffic-driving work anymore.

What exists today:
- Full birth-chart calculation engine (real ephemeris, Lahiri ayanamsa, whole-sign houses, Vimshottari dasha) — astronomically correct, verified against reference values earlier in development.
- Free preview + two-tier paid reading ($25 / $35, $15 upgrade path from $25→$35), Stripe Checkout wired and tested live.
- Book-style paginated reading UI with a real, data-driven chart diagram (not decorative).
- A public `/sample` page showing a full example reading with no signup.
- `/about` and `/faq` pages with honest methodology and no fabricated credentials.
- Gift-purchase framing, a fault-based refund policy (narrowed from an earlier unconditional 14-day guarantee — see §11) surfaced at point of purchase, and a founder's-note trust section on the landing page.
- One full round of independent, simulated cold-visitor QA (three personas) with all findings addressed — see §8.

**Update (post-handoff):** the production domain is now settled as `littlestargazer.com` (singular — `littlestargazers.com`/`.org` were both already registered by others), registered via Cloudflare, with Cloudflare Email Routing forwarding `contact@littlestargazer.com` to the founder's personal inbox. Site copy, `NEXT_PUBLIC_SITE_URL`, and the Privacy/Terms pages below were all updated to match — the visible "Little Stargazers" brand name is unchanged, only the domain/email are singular.

**Update (post-handoff): reading-engine depth pass.** Founder feedback after launch-checklist work: readings read as too generic, with content that varied only by which sign a lead planet sat in regardless of how strong that placement actually was. Addressed in two phases (a third, the D24 divisional chart, was scoped but explicitly deferred by the founder as unnecessary):
- **Phase 1** (`src/lib/astro/aspects.ts`, `src/lib/education/narrative.ts`): added Parashari drishti (aspects) and conjunction detection, and rewrote `metrics.ts`/`subjects.ts`/`direction.ts` around graded tiers (flourishing/steady/growing) driven by real score rather than a binary top-N/bottom-N split, with citations that name house, dignity, conjunctions, and aspects, plus interpretation text (not just the citation) that changes with *why* a placement is strong — a placement's exaltation or a specific conjunction partner now changes the actual sentence, not just a technical prefix. After founder feedback that citations made the text too long, `citePlacement`/`citeHouseLord` were tightened to a compact clause ("Jupiter sits in Virgo, 2nd house, aspected by Moon.") instead of a full sentence.
- **Phase 2** (`src/lib/astro/yogas.ts`, `src/lib/education/yogas.ts`): detects four classical yogas (Gajakesari, Budha-Aditya, Saraswati, Neecha Bhanga) and surfaces them as a conditional "Special chart combination(s)" chapter in the report, only inserted when at least one is found. Important calibration: tested detection across ~50 sample charts before finalizing copy — Budha-Aditya fires in roughly half of all charts (a structural fact of Mercury's orbit, not rarity), while Saraswati/Neecha Bhanga are meaningfully rarer (~1 in 8); content is worded to match each yoga's actual frequency rather than uniformly claim scarcity.
- **Explicitly deferred, by the founder's own call:** Phase 3, a D24 (Siddhamsha) divisional-chart calculation. This would have been genuinely new astronomical calculation (not pattern-matching over already-computed data, unlike Phase 2) and would have warranted the same reference-value verification the original ascendant calculation got. Revisit only if the founder raises it again.

**Update (post-handoff): magic-link email capture shipped and verified live.** See §10 for full detail. Buyer emails are now captured from Stripe Checkout, and a parent can recover a paid reading's link at `/resend-reading` (footer: "Lost your reading link?"). Resend is wired in and confirmed working via a real production purchase + resend test. One rollout incident worth knowing about if a future migration goes out: shipping the new `customer_email` column briefly broke all report pages, because the Supabase migration file was pushed but never actually run against the live database — see §10's callout for the exact failure mode and fix.

**Update (28 Aug 2026): pre-launch housekeeping batch, and Stripe went fully live.** See §11 for full detail. Stripe cleared manual review and is now processing real payments (§8 item 4 done). Separately, a founder-requested batch shipped: the refund policy was narrowed (no longer an unconditional 14-day guarantee), the resend-reading link was made more visible, a `/support` contact form was added, Dependabot was turned on, and Sentry error alerting is wired in and confirmed working live (with a real rollout bug along the way — `instrumentation.ts` needed to live in `src/`, not the project root — see §11's callout).

**Update (28 Aug 2026): blog/SEO shipped, Google Search Console live, brand mark changed, two founder's-brother UX fixes shipped.** See §13, §14, §15 for full detail — condensed here:
- Content/SEO (§8 item 8) is **done**: 6 cornerstone blog posts live at `/blog`, wired into the sitemap. Google Search Console is verified (DNS TXT via Cloudflare) and the sitemap is submitted — see §13.
- **Brand name collision investigated and resolved, logo changed.** A near-identical-named children's astrology brand (`littlestargazers.org`, a picture-book series, no social presence, pre-launch) was found. An IP Australia trademark search came back clean. Decision: keep the "Little Stargazers" name, but change the visual mark (the sparkle icon looked too similar to the other site's) to a distinct "Growth Path" icon (sprout + rising star) — see §14 for the full rationale and what changed.
- **Two pieces of real user feedback (the founder's brother) diagnosed and fixed**, both shipped to production: (1) every reading insight led with technical chart-citation jargon before the actual point — reordered in one shared function; (2) the landing page hero read as a generic AI-generated SaaS template — rebuilt as a full-bleed constellation/night-sky design. See §15.
- **Pinterest (§8 item 9) is in progress, not done** — all creative assets are ready (pins, profile logo, copy) but the actual Pinterest Business account, site verification, and board/pin uploads are founder-only actions still pending. See §16 for where this stands — **paused, not the current priority; see §18.**

**Update (29 Aug 2026): SEO Phase 0-2 shipped (13 blog posts total), two content-engine bugs found and fixed, a real Stripe unlock bug found and fixed, and a conversion-quality test came back at 0/3 — this is the current priority.** Full detail in the new sections below:
- §17 — SEO structured data/byline/internal-linking, real proprietary yoga-frequency data folded into the pillar post, and 7 new blog posts (13 total). Done, shipped.
- §18 — **read this first.** A 3-persona conversion test (age-4, age-12, age-17 parents) found 0/3 would pay, converging on one structural finding: age-tailoring in the reading is cosmetic (a header/label), not real. Full verbatim findings preserved so nothing gets lost — this is the next task.
- §19 — two content-engine bugs the persona test surfaced (duplicate citation text; a Subjects/Direction chapter contradiction). Both fixed already, kept here as a record and for the architectural lesson.
- §20 — a real, non-obvious Stripe checkout bug (a database read-after-write race, not a caching or config issue as it first appeared) found and fixed during a separate live-testing session with the founder. Fixed, shipped, verified.

**Update (31 Aug 2026): a dedicated "answer the parent's actual question" chapter, a Dashamsha (D10) chart, and a real career-significator library built from three classical sources — on a working branch, not yet on production.** Full detail in §21. Following straight on from that work, a content-consistency/repetition audit is now the current priority — §22, **read this first** on top of §21 for context.
- §21 — `directAnswer.ts` (new chapter answering the parent's decision-focus question directly), `divisional.ts` (new D10/Dashamsha chart), and a from-scratch, three-source-grounded `careerSignals.ts` replacing the old one-score-for-all-STEM-careers approach; 9 new career fields; two real bugs found and fixed along the way (a keyword-shadowing bug, a tone-vs-tier-label contradiction). All on `claude/handoff-task-review-umvc5i`, not yet merged/pushed to production.
- §22 — the content-consistency/repetition audit §21 called for. Findings and fixes are in §23.

**Update (31 Aug 2026, same working branch): the §22 audit ran, and its three real findings are fixed.** Full detail in §23 — condensed here: built a repeatable audit script (80 randomized charts through the real report-building code), which found and quantified (1) a near-universal duplicate-sentence bug in `metrics.ts`'s `ROLE_CLAUSE` (96% of sample reports had a byte-identical sentence appear twice), now fixed with real phrasing variants; (2) a tone-vs-tier contradiction in `narrative.ts`'s `EXALTATION_INTENSIFIERS` firing off a lead planet's own dignity regardless of the section's overall computed tier (~30-35% of sample reports), now gated to the flourishing tier only, plus two `CONJUNCTION_FLAVORS` sentences that had survived an earlier spot-fix of the same bug class, now rewritten; (3) confirmed subject-vs-career-field score divergence (22-35% of sample reports, across the exact pairs §22 named) is real and now visibly acknowledged in the direct-answer copy rather than left to read as a silent contradiction. A smaller, lower-priority repetition-ceiling residual (stellium charts occasionally still repeat a `CONJUNCTION_FLAVORS`/citation sentence) remains, quantified but not eliminated — see §23's "what's left" for the honest state.

What does **not** exist yet:
- A live Pinterest presence (assets are ready — see §16 — but no account/boards/pins are live yet).
- Guest-post backlinks, Reddit/Facebook-group organic mentions, or directory submissions (copy/drafts are ready — see §16 — none posted yet).
- Marketing email capture or a nurture sequence (the transactional "resend my reading" magic-link flow has since been added post-handoff — see §10 — but that's deliberately not a mailing list).
- Analytics (privacy-respecting analytics via Vercel Analytics has since been added post-handoff — see update above).
- Privacy Policy / Terms of Service pages (added post-handoff — see update above).
- A confirmed-complete Stripe business-profile/description audit (flagged as a to-do; only the founder can do this, it requires dashboard access).

---

## 3. Tech stack & architecture

- **Framework:** Next.js 16.3.2 (App Router, Turbopack), React 19, TypeScript, Tailwind CSS v4.
- **Astro calculation:** `astronomy-engine` npm package (real ephemeris) + hand-built Lahiri ayanamsa / whole-sign house / Vimshottari dasha logic in `src/lib/astro/`.
- **Persistence:** Supabase (Postgres), service-role key, server-only access — no client-side Supabase, no user accounts/login. A report is a row with a shareable UUID link.
- **Payments:** Stripe Checkout, inline `price_data` (no pre-configured Stripe Price objects), dual-path confirmation (immediate verify-on-redirect + webhook as source of truth).
- **Geocoding:** Open-Meteo (free, keyless) with a small bundled offline fallback city list (`src/lib/geo/fallback-cities.ts`) if the live API is unreachable.
- **Everything currently runs on free tiers** (Vercel, Supabase, Stripe test/low-volume). No paid infrastructure yet.

### Key files
| Area | Path |
|---|---|
| Chart calculation | `src/lib/astro/` (`chart.ts`, `ayanamsa.ts`, `dasha.ts`, `dignity.ts`, `aspects.ts` (drishti/conjunctions), `yogas.ts` (classical combinations), `constants.ts`, `types.ts`) |
| Education/content engine | `src/lib/education/` (`engine.ts`, `pathway.ts`, `subjects.ts`, `direction.ts`, `metrics.ts`, `scoring.ts`, `narrative.ts` (shared citation/tier/composition helpers), `yogas.ts` (special-combination content), `remedies.ts`) |
| Pricing | `src/lib/pricing.ts` |
| Supabase persistence | `src/lib/reports/store.ts`, `src/lib/supabase/` |
| Stripe | `src/lib/stripe/server.ts`, `src/app/api/stripe/webhook/route.ts`, `src/app/report/[id]/actions.ts` |
| Report intake | `src/app/actions.ts`, `src/components/ReportFlow.tsx`, `src/components/PlaceAutocomplete.tsx` |
| Reading UI | `src/components/ReportView.tsx`, `BookReader.tsx`, `pathwayPages.tsx`, `KundliChart.tsx` |
| Routes | `src/app/page.tsx` (landing), `src/app/report/page.tsx` (intake), `src/app/report/[id]/page.tsx` (result), `src/app/sample/page.tsx`, `src/app/about/page.tsx`, `src/app/faq/page.tsx`, `src/app/resend-reading/`, `src/app/support/`, `src/app/blog/page.tsx` + `src/app/blog/[slug]/page.tsx` (§13) |
| Blog content | `src/lib/blog/posts.ts` (metadata registry), `src/content/blog/*.tsx` (one file per post body, plain JSX prose matching `/about`/`/faq` style — no MDX dependency in this repo) |
| DB schema | `supabase/migrations/*.sql` |
| Email (Resend) | `src/lib/email/resend.ts`; used by `src/app/resend-reading/actions.ts` and `src/app/support/actions.ts` |
| Error alerting / ops | `src/instrumentation.ts`, `src/instrumentation-client.ts`, `src/sentry.server.config.ts`, `src/sentry.edge.config.ts` (all must live under `src/`, not the project root — see §11's rollout-bug callout), `.github/dependabot.yml` |
| Brand mark / icons | `src/components/icons.tsx` (`GrowthPathIcon` is the current logo mark — see §14; `SparkleIcon`/`SproutIcon` etc. remain for decorative use elsewhere, not the logo), `src/app/icon.tsx` + `src/app/apple-icon.tsx` (code-generated via `next/og`'s `ImageResponse`, this Next version's convention — see `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/app-icons.md` before touching) |
| Landing page hero | `src/app/page.tsx` (hero section only was redesigned — §15), `src/components/ConstellationSky.tsx` (decorative night-sky SVG, hardcoded coordinates for hydration-safety, same convention as `ChartWheel.tsx`) |
| Reading insight composition | `src/lib/education/narrative.ts`'s `renderTieredInsight` — shared by `subjects.ts`, `metrics.ts`, `direction.ts`; composes every subject/strength/growth/direction sentence. Order matters here — see §15. |

### Environment variables (`.env.example`)
Grown since this document was first written — `.env.example` is the source of truth, kept in sync with comments explaining where each value comes from. As of this update: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` (all live-mode as of §8 item 4), `RESEND_API_KEY`, `RESEND_FROM_EMAIL` (§10), `SENTRY_DSN`, `NEXT_PUBLIC_SENTRY_DSN`, optionally `SENTRY_ORG`/`SENTRY_PROJECT`/`SENTRY_AUTH_TOKEN` (§11), and `NEXT_PUBLIC_SITE_URL`. All confirmed working in production.

### A sandbox quirk worth knowing
This project has been developed inside a sandboxed environment whose network egress blocks Supabase and Vercel preview/custom domains directly. This is **not a production issue** — it only affects testing from inside that specific sandbox. The established workaround, used repeatedly during development, is a temporary `src/app/dev-preview/page.tsx` route that renders the same components with locally-computed sample data (no DB write), screenshotted via Playwright, **always deleted before committing**. If a future agent hits `EGRESS_BLOCKED` or a Supabase "host not in allowlist" error while testing locally, this is why — it doesn't mean anything is broken in production.

---

## 4. Business model & pricing

- **Tier 1 — "The Guiding Stars Reading" — $25**: full personalized learning pathway (subjects, natural direction, life-chapter timeline, ideal learning environment, weekly rhythm).
- **Tier 2 — "The Complete Constellation Reading" — $35**: everything in Tier 1 plus gentle, traditional remedies personalized to the child's chart (explicitly "no gemstones, nothing prescriptive").
- **Upgrade path**: a parent who already bought Tier 1 can add remedies for **$15** (not the full $35) — this required a deliberate branch in the Stripe Checkout logic (`src/app/report/[id]/actions.ts`) to detect "already owns full, upgrading" vs. "fresh purchase."
- **Pricing is per report/child, not per account** — there are no accounts. A parent with three kids pays three times.
- Positioning: priced above commodity astrology reports ($6.95-class templated products) and below the boutique/human-design category ($47-125), deliberately in USD (not ₹) to support the "appeals to any parent, not just Indian" goal.

---

## 5. Market research summary

Two rounds of research were done and are worth reading in full before making channel/positioning decisions:

1. **Full market assessment** (competitive landscape, market sizing, financial scenarios, risk register) — published as an artifact: **https://claude.ai/code/artifact/38336085-9165-41c9-86b7-eeca96b86bdc**

Headline findings, condensed:
- The niche (Vedic astrology × child-specific × global/non-jargon tone × modern UX) is real but narrow — every direct competitor found (AstroKids.ai, Astro Mentor Kids, Cafe Astrology Kid Zone, STAR/CHILD, India's "Education Report" vertical) fails at least one of those dimensions. Little Stargazers, as built, is the only product that clears all five — that's the actual competitive gap, not a vague "big market" claim.
- Realistic passive-income range: **$300–$2,000/month within 12–18 months** of consistent content effort. Not instant, not venture-scale.
- **Paid ads are a poor channel fit** — Meta treats astrology as a sensitive category with restricted targeting; margins at a $25–35 price point don't survive typical CPCs at a 1–3% conversion rate. **Organic content + Pinterest is the identified primary channel** (case studies in the research show outsized performance for parenting/spiritual content specifically).
- **Payment processor risk**: Stripe's restricted-business rules name "psychic services"/"occult material." The mitigation already built in is that site copy avoids prediction/fortune-telling framing throughout (favors "education," "parenting guidance," "personality insight"). Paddle explicitly bans horoscopes outright — ruled out as an alternative. Lemon Squeezy is untested/ambiguous. **Recommendation: stay on Stripe**, audit the Stripe dashboard business description for trigger words (manual, founder-only action), and revisit specialty processors only if real volume ever triggers a flag.

---

## 6. Product/ethical constraints (read before writing any marketing copy)

Established explicitly and held throughout development — a new agent should not relax these without the founder raising it again:

- **No fabricated reviews or testimonials.**
- **No fake urgency or scarcity** (countdown timers, "X spots left," fake buyer counts).
- **No fear-based framing** ("your child will struggle without this").
- Legitimate, bolder persuasion (specificity, emotional warmth, confident CTAs) is explicitly encouraged and has already been applied to the landing page and pricing copy.
- Rationale is legal, not just moral: Australian Consumer Law bans the first two outright for an AU-based business, with penalties up to $50M or 30% of turnover for a company; the third is also the exact profile of complaint that drives chargebacks, which threatens the Stripe relationship directly.
- The `/about` and `/faq` pages deliberately contain **no invented founder bio or fake credentials** — "small independent team" is stated plainly rather than fabricated authority. Keep it that way unless a real name/credential is provided by the founder.

---

## 7. QA history: three-persona critique (do this again before major changes)

A cheap, high-value technique used once already, worth repeating before future major launches: spin up 2-3 agents with genuinely different mindsets, each browsing the live site cold, in character, deciding for themselves whether to continue or bounce, instructed to be harsh rather than polite.

**Personas used:** a skeptical non-believer (scam-radar primed), an experienced/discerning astrology believer (compares against real paid readings), and an impatient mobile parent with a 4-minute window. All three were run via Playwright against a local dev server (their own worktrees, no source-code reading allowed, screenshots saved).

**Result: 1 of 3 would have bought** (the believer, $35 tier) — driven by the two chapters that actually impressed her (the dasha timeline, the remedies) turning out to be exactly what's paywalled, not padding around it. The other two had specific, fixable reasons, not "the idea doesn't work."

**Bugs found and fixed (commit `c6cb582`):**
1. Chapter navigation didn't reset scroll position — a visitor could land on the paywall chapter mid-scroll, past the cheaper tier, straight onto the pricier upsell card. Fixed in `BookReader.tsx` (scroll-into-view on page change).
2. A failed form submission silently wiped Name/DOB/Time (React's automatic form-reset behavior on uncontrolled fields). Fixed by making those fields controlled state in `ReportFlow.tsx`.
3. A save failure showed the raw exception message (including internal infra detail) to the parent. Fixed by splitting chart-computation and save into separate try/catch blocks in `src/app/actions.ts`, with a generic friendly message on save failure.

**Content gaps found and fixed (same commit):**
4. Chart-citation specificity faded in the back-half chapters (subjects support, direction, environment) — fixed by weaving real placement citations into `subjects.ts`, `direction.ts`, `pathway.ts`.
5. The "natural direction" chapter named specific career fields in a way that read as prediction rather than possibility — reframed with explicit "not a prediction" language.
6. No chart visual existed anywhere (all decorative) — built `KundliChart.tsx`, a real South-Indian-style chart diagram driven by the child's actual placements.
7. No credibility page existed — added `/about` and `/faq`.
8. Price wasn't visible until chapter 6 of the reading — surfaced on the landing page hero and above the intake form submit button.

All fixes were re-verified against a live reproduction of the original failures (not just read in code) before committing.

---

## 8. Launch checklist — do these next, in order

### Before any traffic-driving work (cheap, mostly founder-only actions)
1. ✅ **Done.** Real, monitored inbox set up: `contact@littlestargazer.com` (Cloudflare Email Routing, forwarding to the founder's personal inbox). All site copy updated to match (footer, `/about`, `/faq`, `/privacy`, `/terms`).
2. ✅ **Done.** Production domain settled: `littlestargazer.com` (singular — both `littlestargazers.com` and `littlestargazers.org` were already registered by others). `NEXT_PUBLIC_SITE_URL` set accordingly.
3. ✅ **Done.** Privacy Policy (`/privacy`) and Terms of Service (`/terms`) pages added, linked in the footer.
4. ✅ **Done (27 Aug 2026).** Stripe cleared manual review and is now live: `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` in Vercel are live-mode values, a live webhook endpoint is registered (Stripe Dashboard → Workbench → Webhooks, subscribed to `checkout.session.completed`), and the founder verified the full path with a real purchase — report unlocked, webhook delivery confirmed `200`. The business-profile/trigger-word audit from the original checklist item wasn't separately itemized, but nothing in the verified flow surfaced an issue.
5. ✅ **Done.** Privacy-respecting analytics added (Vercel Analytics — cookie-free, no consent banner needed).
6. ✅ **Done.** `sitemap.xml` and `robots.txt` added (Next.js app-router conventions, `src/app/sitemap.ts` / `src/app/robots.ts`), private `/report/[id]` links excluded from both.

### Tier 2 — the actual growth engine (deferred by founder's own choice until the above + this QA round were done)
7. **Email capture** at intake, stored in the existing free-tier Supabase (no paid ESP needed yet) — pick a tool (free-tier Resend/Buttondown) and write real opt-in/unsubscribe copy (Australia's Spam Act applies). Not started.
8. ✅ **Done (28 Aug 2026).** Content/SEO engine: 6 cornerstone blog posts shipped at `/blog`, each linking to `/sample`. See §13.
9. **Pinterest presence — in progress, see §16.** Assets (6 pins, profile logo, copy) are ready; the actual account/verification/upload is a founder-only action not yet done.
10. **A "gift a reading" flow refinement** — the gift *framing* already exists (checkbox on intake, adjusted copy); a dedicated purchase-as-gift flow with a gift note field would be a natural next iteration once traffic exists to justify it. Not started.

### Explicitly deferred (correctly, by the founder's own earlier call)
- Physical product upsells (learning toys, "planet-supporting" items) — revisit only once the digital funnel is proven.
- Specialty/high-risk payment processors — only if Stripe ever actually flags the account or volume justifies the higher fees.
- Any live-astrologer/consultation feature — deliberately excluded; it's the labor-heavy model this project is explicitly trying to avoid in favor of a low-touch, content-fed, passive-income shape.

---

## 9. Working conventions established this session (for whoever picks this up)

- **Screenshot every new UI before shipping.** The pattern: temporary `dev-preview` route with locally-computed sample data → Playwright (`npm install -D playwright`, then `npm uninstall playwright` before committing) → visually verify → delete the temp route → commit.
- **Never commit the temp QA route or scratch scripts.** Check `git status` clean before every commit in this project.
- **Build + lint clean before every commit**: `npm run build && npm run lint`.
- **Push after every commit** to `claude/vedic-horoscope-learning-site-fb6fta` (`git push -u origin claude/vedic-horoscope-learning-site-fb6fta`).
- **`claude/vedic-horoscope-learning-site-fb6fta` is this repo's only branch and its default/HEAD branch** — there is no separate `main`. Everything pushed here is what Vercel deploys. No merge/PR step is needed; committing and pushing to this branch *is* shipping to production.

---

## 10. Magic-link email capture for paid readings — done, verified live end-to-end

**Status: fully shipped and confirmed working in production** (commits `a3fd690`, `d56b326`, `f2bda45`). The founder confirmed Resend as the email provider before any API key was wired in (a new third-party service with access to customer email addresses). Domain verification, API key, and env vars are all in place; the founder ran a real production test — an actual purchase, then a resend request against that same email — and the email arrived. Nothing further to do on this feature.

What shipped:
- `supabase/migrations/0003_add_customer_email.sql`: nullable `customer_email` column on `reports`.
- The Stripe webhook (`checkout.session.completed`) now reads `session.customer_details?.email` and persists it via `setReportCustomerEmail` (`src/lib/reports/store.ts`), stored lowercased. The best-effort redirect-verification path (`report/[id]/page.tsx`) is untouched — the webhook is the trusted source, as it already was for `tier`.
- `src/lib/email/resend.ts`: a thin wrapper over Resend's HTTP API (no SDK).
- `/resend-reading` page + `resendReadingAction` (`src/app/resend-reading/`): a parent enters their checkout email, the action looks up paid reports (`findPaidReportsByEmail`, tier-not-null only) and emails the direct link(s) found. Linked from the footer as "Lost your reading link?".
- **Security, as specced:** the response is the same generic message ("If we have a paid reading on file for that email, we've sent the link(s) to your inbox.") whether or not a match was found, mail is only ever actually sent on a real match, and there's a best-effort in-memory per-email throttle (documented in-file as hygiene, not a hard guarantee, given serverless cold starts). Verified locally: a Supabase lookup failure still degrades to the same generic success message rather than leaking an error.
- `/privacy` updated with a short, narrowly-scoped disclosure (resend-only, explicitly not marketing, no mailing list) and Resend added to the "who else sees this" list.
- Screenshotted locally via the dev-preview + Playwright pattern per §9 before shipping; build + lint clean; pushed.

**⚠️ Production incident during rollout, and the lesson for future agents:** shipping commit `a3fd690` broke the site briefly — every report (new and existing) started 404ing with "We couldn't find that reading." Root cause: `supabase/migrations/0003_add_customer_email.sql` was committed and pushed, but **migration files in this repo don't auto-apply to the live Supabase database** — nothing in the deploy pipeline runs them. `getReport()` was updated to `select` the new `customer_email` column before that column existed in production, so every read failed. Fixed by the founder running the migration's SQL directly in the Supabase SQL Editor (no redeploy needed, since Next.js/Vercel doesn't rebuild for a DB-only fix). **Takeaway: any future migration must be flagged as a manual, out-of-band step the founder has to run *before* (or atomically with) a deploy that reads/writes the new column — call it out explicitly, don't assume it's applied just because the SQL file is in the repo and merged.**

---

## 11. Pre-launch housekeeping batch (27 Aug 2026) — refund policy, support, monitoring

Founder-requested batch, done before starting on SEO/Pinterest (§8, Tier 2). Commits `34aa742`, `3fb6cf0`, `bf01962`.

**Refund policy narrowed.** The founder's concern: the old "full refund within 14 days, no questions asked" was easy to abuse on a digital product that's viewable in full immediately after purchase. Replaced across the footer, the paywall CTA (`ReportView.tsx`), `/faq`, and `/terms` with a narrower policy: genuine faults (technical issues, a reading that wasn't generated correctly) get made right, but change-of-mind isn't covered. Deliberately **not** worded as "no refunds, all sales final" — `/terms` explicitly preserves Australian Consumer Law guarantees, which can't be excluded by a stated policy anyway; claiming otherwise would itself risk being a misleading statement about consumer rights, the same ACL sensitivity this project has been careful about throughout (see §6).

**Resend-link visibility.** The `/resend-reading` "Lost your reading link?" link already existed in the footer (§10) but the founder hadn't noticed it there. Added a second, more prominent copy directly on the `/report` intake page ("Already bought a reading? Get your link resent") — the more natural landing spot for someone who thinks they've already purchased. Footer link kept as-is.

**New `/support` page** (`src/app/support/`, `SupportForm.tsx`): a simple contact form, no new third party — reuses the existing Resend integration to email `contact@littlestargazer.com` with `reply_to` set to the visitor's address (so replying in the founder's inbox goes straight back to them), plus a honeypot field and the same best-effort per-email throttle pattern as `/resend-reading`. Linked from the footer.

**Live chat / WhatsApp — explicitly decided against, for now.** The founder considered WhatsApp for support but didn't want to expose a personal number (it's their personal account, not a business line). Options discussed: personal WhatsApp (exposes personal number, rejected), a separate WhatsApp Business number (viable later if support volume justifies the setup), a live-chat widget like Tawk.to (new third party, visitor chat content). **Decision: support form only, for now.** Revisit only if support volume or founder preference changes — don't add WhatsApp/live-chat on your own initiative.

**Dependabot added** (`.github/dependabot.yml`): weekly npm dependency checks, minor/patch bumps grouped into one PR to keep noise down. Major version bumps (e.g. a future Next.js major) will surface as their own PR — review those deliberately against `node_modules/next/dist/docs/` for the target version before merging, per `AGENTS.md`'s warning that this project pins a Next.js version with real behavioral differences from stock docs/training data.

**Sentry wired in for error alerting — done, verified live end-to-end.** Founder confirmed this third party before it was added (same diligence as Resend). `@sentry/nextjs` with server/edge/client init (`src/sentry.server.config.ts`, `src/sentry.edge.config.ts`, `src/instrumentation-client.ts`, `src/instrumentation.ts`), plus explicit `Sentry.captureException` calls added to the error paths that are deliberately caught-and-swallowed before they'd ever reach Next's automatic `onRequestError` hook: chart computation and report-saving in `src/app/actions.ts`, and the catch blocks in `/resend-reading` and `/support`'s actions. The founder created a Sentry project, set `SENTRY_DSN`/`NEXT_PUBLIC_SENTRY_DSN` in Vercel, and confirmed real delivery with a deliberately-thrown test error — Sentry both recorded the issue and sent an email alert.

**⚠️ Rollout bug and the lesson for future agents:** the first version of this had a real bug, not just a missing env var. `instrumentation.ts` and `instrumentation-client.ts` were placed at the **project root**, but this codebase puts everything under `src/` (`src/app`, `src/lib`, `src/components`) — and Next.js only loads those two special files from wherever `src/app` lives, i.e. **`src/instrumentation.ts`**, not the root. With them in the wrong place, Next.js silently never loaded them: `Sentry.init()` never ran, with zero errors or warnings anywhere (build succeeded, lint passed, the app worked fine) — the only symptom was the Sentry dashboard staying permanently empty. Diagnosed by temporarily adding `debug: true` to `sentry.server.config.ts` and a throwaway `/api/sentry-test` route that deliberately threw: even with debug logging on, *nothing* Sentry-related appeared in the Vercel function logs, which was the tell that `register()` was never being called at all (a real init failure, even with a bad DSN, still logs plenty). Fixed by moving all four files into `src/` together (their relative imports to each other still resolve, since they moved as a group). **Takeaway: any Next.js special file (`instrumentation.ts`, `middleware.ts`, etc.) must go inside `src/` in this project, matching where `src/app` lives — check this specifically whenever adding one, since Next.js fails silently rather than erroring when the file is in the wrong place.**

**Uptime monitoring — recommended, not implemented (dashboard-only, no code needed):** sign up for UptimeRobot's free tier, add monitors for `https://littlestargazer.com` and `https://littlestargazer.com/report`, set an email alert contact. Sentry catches *application* errors (a chart failed to generate); this catches the site being unreachable at all — the two are complementary, not redundant.

**Pricing — recommendation given, no change made.** Founder asked whether $25/$35 is competitive or should go cheaper. Recommendation: hold current pricing — it's deliberately positioned between commodity template reports (~$6.95) and boutique readings ($47–125) per the market research (§5), and cutting price now, before any real traffic exists to show whether price is actually the objection, trades away that positioning on a guess. If a lever is wanted for the first wave of buyers, a genuine, disclosed, time-boxed launch discount (e.g. "$19 for the first 2 weeks") is a better tool than a permanent cut, and doesn't conflict with the site's no-fake-urgency stance (§6) as long as it's real and honestly stated. Revisit with actual checkout-abandonment data once there's traffic (Vercel Analytics is already running).

**Context this was built against (kept for reference):** the founder had asked about full accounts/login so parents could return to a saved reading. That was discussed and deliberately rejected in favor of something lighter — the product's "no accounts, ever" positioning is load-bearing (it's in the footer, `/privacy`, `/terms`, `/faq`, and was cited in the market research as a differentiator), and full accounts would mean rewriting all of that copy for a problem a much smaller mechanism solves just as well. Free-tier (unpurchased) reports were deliberately left out of scope — there's nothing to protect access to on those.

**Don't conflate this with launch-checklist §8 item 7** (the Tier-2 "email capture + nurture sequence" for marketing, which needs real opt-in/unsubscribe copy under Australia's Spam Act). This magic-link feature is transactional (delivering something the customer already bought), a much lighter compliance bar — keep them as separate features, and don't let this one grow into a marketing list without building proper consent first.

**Confirmed 27 Aug 2026:** Stripe is now live (see §8, item 4). The webhook path and email capture were first verified against test-mode Checkout, then re-verified against a real live purchase after the switch — report unlocked, webhook delivery `200`. Nothing left to re-check here.

---

## 12. (superseded — see §13 onward for what shipped and §16 for the current next task)

The content/SEO + Pinterest brief that used to live in this section is done or in progress — see §13 (blog/SEO + Search Console) and §16 (Pinterest + outreach, the actual next task).

---

## 13. Content/SEO engine + Google Search Console — done

**Status: shipped and live.** Commits: blog section (`Add cornerstone blog section for SEO/content marketing`), Search Console setup was founder-driven (dashboard-only, no code).

**What shipped:**
- `/blog` (listing) + `/blog/[slug]` (post template) — see the Key Files table above for exact paths. No MDX dependency: each post is a plain `.tsx` file under `src/content/blog/` exporting a `Post()` component with JSX prose, matching the existing `/about`/`/faq` style. The `[slug]/page.tsx` route holds an explicit `POST_CONTENT` registry mapping slug → dynamic `import()` — deliberate, not a fully-dynamic `import(`...${slug}`)`, since that pattern breaks static analysis/bundling.
- 6 posts, each tied to one real chapter of the actual product (moon sign/temperament, ideal learning environment, Vimshottari dasha, subject choice, new-baby readings) plus one pillar/overview post — topics were spot-checked against real search demand via web search before committing (moon-sign/zodiac-parenting content is an actively published, populated niche) rather than guessed.
- Wired into `src/app/sitemap.ts` (each post gets its own sitemap entry) and the footer nav (`src/app/layout.tsx`).
- All copy follows §6's constraints (no fake urgency, no fear-framing) and deliberately avoids "psychic/fortune-telling" framing (Stripe's restricted-category sensitivity, §5) — same discipline as the rest of the site.

**Google Search Console — done by the founder, dashboard-only:**
- Verified `littlestargazer.com` as a **Domain property** via DNS TXT record added directly in Cloudflare (the domain's existing registrar/DNS provider, per §2's domain update).
- Sitemap (`https://littlestargazer.com/sitemap.xml`) submitted.
- Priority URLs (`/`, `/blog`, `/sample`, a couple of blog posts) manually queued via "Request Indexing."
- **Why this was necessary, for context**: the site had zero indexed pages anywhere (confirmed via web search) despite no technical blocker (`robots.ts` correctly allows crawling, no stray `noindex`) — purely because the domain is new, had zero backlinks, and nothing had told Google it existed yet. Expect indexing to take roughly 1-4 weeks from here, not immediately, even now that this is done.

---

## 14. Brand name collision investigated, name kept, logo mark changed

**Status: resolved.** Commit: `Replace sparkle logo mark with Growth Path icon`.

**What happened:** the founder's own manual browsing turned up `littlestargazers.org` — a **different business** (an illustrated children's picture-book series about the planets, "A Children's Astrology Adventure," pre-order/pre-launch stage) using the **exact same brand name**, "Little Stargazers," plus a visually similar identity (sparkle icon, navy/gold night-sky palette). Worth knowing this repo already recorded that `littlestargazers.org` (and `.com`, plural) were "already registered by others" back when the domain was chosen (§2) — this session is the first time anyone actually looked at what was live on that domain.

**Investigation done, in order:**
1. Confirmed via web search that the *.org* site is real, live, and uses the identical brand name and a similar sparkle/navy/gold visual identity.
2. Checked for a registered trademark: the founder ran an **IP Australia trademark search** for "Little Stargazers" directly (the authoritative source — general web search can't see trademark databases) and it came back **clean, no registration found**.
3. Checked for social-media presence under the name for the other business (a proxy for how much real public goodwill/reputation exists to actually collide with) — the founder checked directly (this session's own attempts were blocked; this environment's network policy blocks direct fetches to Instagram/Facebook/TikTok/the target domain itself, a hard tool limitation, not inconclusive evidence) and found **none** — only unrelated "Star Gazer"-named accounts.

**Decision and rationale:** given a clean trademark search and no established social/public presence on the other side, the practical confusion/legal risk is low. **Decision: keep the "Little Stargazers" name** (a full rebrand — new domain, Stripe re-verification, SEO reset — would be a much larger cost than the risk justifies), but **change the visual mark** since the sparkle-icon-on-navy-and-gold look was the most strikingly similar element and is cheap to change on its own.

**What changed:** a new `GrowthPathIcon` (sprout + small rising star, in `src/components/icons.tsx`) replaces the sparkle as the actual **logo** — header + footer wordmark (`src/app/layout.tsx`), the browser tab icon and iOS home-screen icon (`src/app/icon.tsx` / `src/app/apple-icon.tsx`, using this Next version's `next/og` `ImageResponse` convention — read that doc file before touching icon files again), and all outward-facing marketing assets (Pinterest pins, profile-photo logo mark). **Deliberately scoped narrowly**: the original sparkle motif (`SparkleIcon`) is used extensively as a *decorative* accent throughout the actual reading content and other pages (report chapters, `/about`/`/faq` kickers) — none of that was touched, since it isn't the brand mark itself and changing it wasn't part of what was asked or needed.

**If a future agent or the founder reconsiders this:** the six alternative icon directions that were sketched and rejected (Compass, Chart Wheel, Telescope, Crescent & Orbit, Nakshatra Dot, plus Growth Path which was chosen) are not saved anywhere in the repo — they were ephemeral scratch renders. If a different mark is wanted later, regenerate from scratch rather than looking for old files.

---

## 15. Founder's-brother UX feedback — two fixes, both shipped

Real, specific feedback from a family member testing the product cold. Both diagnosed against actual code (not guessed at) before fixing. Commits: `Lead reading insights with the actionable point, not the chart citation`, `Redesign landing page hero with editorial night-sky look`.

**Fix 1 — reading order (jargon before insight).** The feedback: *"When showing a reading display the actionable points first... hard for a layman to know what sits in which house."* Root cause found in `src/lib/education/narrative.ts`'s `renderTieredInsight` — the single shared function that composes **every** subject, strength/growth-metric, and natural-direction sentence across the whole reading. It was building each sentence as `[citation, insight, extras].join(" ")` — e.g. *"Mars sits in Scorpio, 6th house, aspected by Saturn. Physical activity looks like a genuine strength..."* — so the technical citation was grammatically always the first thing read. **Fix**: reordered to `[insight, extras, citation].join(" ")` — one line, one shared function, fixes every chapter that uses it (`subjects.ts`, `metrics.ts`, `direction.ts`) at once. Verified against `/sample` (real chart-rendering, no Supabase needed) before shipping, not just read in code.

**Fix 2 — landing page "looks like a generic AI-generated site."** Diagnosed by actually reading `src/app/page.tsx`: the hero was a textbook dark-hero/pill-badge/two-button/icon-in-a-circle-cards template pattern, structurally identical to thousands of Framer/v0-generated SaaS sites, despite having custom copy and colors. **Three redesign directions were mocked up first** (as standalone rendered PNGs, not live code) and shown to the founder before touching real code: (A) real chart-wheel as hero visual with annotated callouts, (B) hero leads with an actual sample insight card instead of marketing copy, (C) full-bleed editorial night-sky illustration, no pill badge, single CTA. **Founder picked (C).** Shipped as: `src/components/ConstellationSky.tsx` (new decorative SVG, hardcoded star/constellation coordinates — same hydration-safety convention as `ChartWheel.tsx`, i.e. no `Math.random()`) + a rewritten hero section in `src/app/page.tsx`. Only the hero section changed — the rest of the landing page (how-it-works cards, stat grid, mission section) still has the same generic-template texture and hasn't been touched; flag this to the founder as unfinished if they don't raise it themselves.

**One judgment call made without being asked, worth knowing about:** the approved mockup for (C) dropped the "See a full sample reading first" link entirely for a cleaner look. It was **kept** anyway (just de-emphasized, small text under the stat line) because §7's own three-persona QA history found that exact link was the deciding factor in the one persona who actually converted. Don't remove it without the founder explicitly asking, even if a future visual refresh suggests it again.

---

## 16. Next task for the incoming agent: execute the Pinterest + outreach plan

**Status: assets are 100% ready; execution has not started.** This is a founder-execution task more than a coding task — most of the remaining work needs the founder's own hands (creating accounts, posting, sending emails), not code changes. The agent's role here is mostly: walk the founder through each step, handle any code-side pieces (see below), and keep the assets/copy organized.

**What already exists, ready to use (all built this session, all using the current `GrowthPathIcon` branding):**
- 6 Pinterest pin images (1000×1500, night-sky style with constellation art, one per blog post) + matching pin titles/descriptions — sent to the founder as files during this session, **not saved anywhere in the repo** (they were scratch-rendered PNGs, not committed). If they're needed again and the founder doesn't still have them, they'll need to be regenerated — the generation approach (a local Node+Playwright script rendering styled HTML to PNG, `npm install -D playwright` → render → `npm uninstall playwright` before any commit, per §9's convention) is described here so it doesn't need to be reinvented, but the actual script content is not preserved.
- A profile-photo logo mark (dark + light versions, 800×800, the `GrowthPathIcon` sparkle-and-sprout mark).
- A full **promotion kit** (also sent as a file, not committed to the repo): Pinterest pin copy, a guest-post pitch email template, Reddit-safe and Facebook-group-safe post drafts, and a directory-submission target list. Same caveat — not in the repo, only delivered to the founder directly.

**What the founder needs to do (in this order, per the priority already agreed with them):**
1. **Pinterest** (highest priority — per §5's market research, the single best-evidence channel for this audience):
   - Create a Pinterest Business account.
   - Claim the website (`littlestargazer.com`) via Settings → Claim → Websites, using the **HTML tag** method.
   - **When the founder gets the verification code from Pinterest, that's this agent's cue to act**: add it to `src/app/layout.tsx`'s metadata (Next's `verification` field pattern, same idea as how Google Search Console could have been done in-code but was actually done via DNS instead) and push, so the founder can click "Verify."
   - Create 4-6 boards, upload the 6 pins with the prewritten copy, join 2-3 relevant group boards.
2. **Reddit + Facebook groups**: start genuine, non-promotional participation now (per the promotion kit's guidance — comment for 1-2 weeks before ever posting a link, to avoid read as spam/getting banned).
3. **Directory submissions**: low-effort, can be done anytime from the list in the promotion kit.
4. **Guest-post outreach**: send 2-3 personalized pitches a week once there's at least a little momentum elsewhere, using the template in the promotion kit.

**Constraints that still apply (same as always):**
- §6 — no fabricated reviews/testimonials, no fake urgency, no fear-framing — applies to every piece of outreach copy, forum post, and pin description.
- §5 — don't relitigate paid ads as a channel without new evidence; Meta's restricted-category rules and this price point's margins were already assessed as a poor fit.
- Any actual code change (the Pinterest verification tag being the main likely one) still needs build+lint clean and a screenshot before shipping, per §9 — this branch has no staging step.

**Not this task's job** (already deferred, separate items — don't fold in without the founder asking): §8 item 7 (marketing email capture/nurture sequence) and item 10 (dedicated gift-purchase flow).

**Status as of this update: paused, not abandoned.** With the conversion-test findings in §18 landing, the founder wants those addressed first — Pinterest execution is still the right next growth move once §18's product-side work is done, but don't start it before §18 without the founder redirecting.

---

## 17. SEO Phase 0-2: structured data, real proprietary data, 13 blog posts total — done

An SEO strategy was drafted as an artifact (**https://claude.ai/code/artifact/141c452b-bd53-4647-930b-f2d4d7ef49ba**) built around one core reframe: Google's 2026 "scaled content abuse" policy targets *purpose* (pages made to manipulate rankings), not *method* (AI-assisted writing isn't penalized on its own). The plan's explicit anti-pattern, worth repeating for any future agent tempted by it: **never generate blog content by sweeping every combination the chart engine can produce** (a post per sign, per house, etc.) — that's the exact shape of the penalty.

**Phase 0 — foundation (commit `2281c4a`):**
- JSON-LD structured data: `Organization` sitewide, `BlogPosting` per post, `FAQPage` on `/faq` (`src/lib/seo/schema.ts`).
- A real named byline ("Jaya," the founder's real first name — see HANDOFF §6's no-fabricated-credentials rule, which this respects: a real name is not a fabricated credential) replacing the anonymous "small team" attribution on blog posts.
- Freshness signals: `updatedAt` field on posts (only bumped on genuine content changes, never cosmetically — see the type comment in `src/lib/blog/posts.ts`), "Last reviewed" dates on `/about` and `/faq`.
- Internal-linking fix: none of the original 6 spoke posts linked back to the pillar post (`vedic-astrology-parenting-guide`) — now a proper hub-and-spoke, pillar links out to every spoke in "Where to go from here," every spoke links back.
- **"Why I built this" section added to `/about`** (commit `7e5850b`) — a real, founder-supplied story (family astrology lineage, using it with her own kids), not an invented backstory. A founder photo was considered and explicitly declined after the one supplied turned out to be AI-generated (watermarked) — see the conversation this session for why that specific line was held firm: a fake photo next to a true personal story would have undercut the entire trust-building point.

**Phase 1 — real proprietary data (commit `1f089de`):** the pillar post's "What's actually being calculated" section now cites the actual yoga-frequency audit from earlier development (Budha-Aditya ~50% of charts, Saraswati/Neecha Bhanga ~1-in-8) as genuine, unique data no competitor site can publish — this is the single highest-leverage move in the whole plan, since it's real information gain rather than another generic explainer.

**Phase 2 — topical expansion, validated not guessed:** a research pass (real web searches, not assumptions) validated demand for 5 additional topics beyond the original 6, explicitly ranked, with one (a hobbies/activities post) deliberately held back because nearly all existing competitor content in that space is the exact sign-by-sign template pattern this plan exists to avoid. Shipped, 13 posts total now live:
1. Original 6 (pillar + moon-sign, environment, dasha, subjects, new-baby) — pre-existing.
2. **Age-banded, requested directly by the founder after parent feedback** that content felt generic past the toddler years (commit `ff21d05`): `middle-school-subject-selection-birth-chart` (ages 11-14 elective decisions) and `senior-year-subjects-university-direction-birth-chart` (teens narrowing toward degree areas — leads with "this isn't a prediction" in its first sentence, the highest-risk post on the site for sliding into fortune-telling framing, per §6).
3. **5 more, from the validated research list** (commit `58a09c0`): `wired-differently-from-your-child`, `highly-sensitive-child-birth-chart`, `why-siblings-turn-out-different-birth-chart`, `mercury-placement-child-communication-style`, `twins-birth-chart-different-personalities`. Two are worth knowing the framing decisions on if touched again: `wired-differently-from-your-child` was deliberately reframed away from its original "parent-child chart comparison" brief because **the product has no parent-chart input and no joint-reading feature** — writing it as if that existed would have been a real overclaim; it's framed entirely around the child's own chart instead. `highly-sensitive-child-birth-chart` is careful to credit "Highly Sensitive Child" as Elaine Aron's real psychological term, never implying the chart diagnoses or detects it.

All 13 posts pass build+lint, were screenshot-verified before shipping, and follow §6's constraints throughout (no fake urgency, no fabricated credentials, never framing anything as predicting a child's future/career as fact).

---

## 18. Conversion-quality test: 0 of 3 realistic parents would pay — READ THIS FIRST, this is the next task

**Why this test happened:** after the age-banded blog posts shipped (§17), the founder asked a direct question — would this actually convert real parents, across different ages, with different mindsets? Rather than guess, three agents each played a specific, realistic parent persona (different child age, different emotional state) and genuinely decided whether to pay, then reviewed the actual paid content to give harsh, specific feedback.

**Methodology (repeatable — use this again after any fix below ships):** since this sandbox's network blocks Supabase and Stripe directly (see §3's sandbox-quirk note), a temporary route was built at `src/app/dev-preview/persona/[personaId]/page.tsx` (never committed — same disposable-route convention as §9) rendering `ReportView` with `tier=null` (locked/free) or `tier="premium"` (fully unlocked) for three fixed, realistic children, using the exact same production code path as `/sample` does. Each persona-agent browsed the real landing page, the relevant blog post, `/about`, `/faq`, and `/report`'s real intake form in character (no source-code reading allowed, to keep it a genuine black-box test), then reached the actual free preview and pricing chapter, decided for themselves whether to click "Unlock" (without actually clicking it, to avoid any live Stripe interaction), then reviewed the `tier=premium` version of the *same* child to give informed, harsh, honest feedback as if they'd just spent their own money. This is a stronger version of the original §7 three-persona QA — same spirit, now testing conversion intent specifically, with real product content on both sides of the paywall.

### Headline: 0 of 3 would have paid

That's worse than the original §7 QA round (which had 1 of 3 buy) — but far more useful, because all three converged independently on the same root cause rather than three unrelated complaints.

### Persona 1 — early years (a 4-year-old, "Ivy"), curious-but-skeptical, no urgency, price-sensitive for "just for fun"

**Verdict: no, neither tier.** Deciding factor was a since-fixed bug (§19, Bug 1) — the exact same citation sentence reused verbatim as "evidence" for four unrelated headline traits, directly contradicting the site's own "not a template" claim.

Other findings, in the persona's own words:
- What worked: "the tone is genuinely disarming for a skeptic" — the About/FAQ candor ("We're not a large astrology company... we don't have a network of astrologers on staff," "Is this scientific? No, and we won't pretend otherwise") "bought real goodwill I wasn't expecting."
- What worked: real age-awareness in one chapter — "Ivy is currently 4 years old. At this age, Ivy's chart is best explored through play rather than formal lessons" is specific, not boilerplate.
- What didn't: "Areas to nurture" — the section that should carry the most weight — gave two items, both hedged to "fairly typical," "neither unusually fast nor unusually effortful," "standard teaching approaches should suit them well." Direct quote: *"'she's about average' twice in a row is a letdown"* for a section pitched as insight into where a child needs support.
- What didn't: the subject-by-subject chapter (the $25 tier's headline sell) resolved 5 of 7 subjects to "fairly ordinary"/"fairly typical" — honest, but undercuts the pitch of concrete subject-by-subject guidance.
- What felt off: the pricing page's hook line ("You already see how bright they are. Go deeper") assumes the free preview already impressed — it didn't, so the emotional beat didn't land.

**Age-fit verdict, in the persona's words:** *"It feels like one age-neutral personality engine with an age-aware wrapper bolted on top, rather than a reading built around a 4-year-old specifically."*

### Persona 2 — middle school (a 12-year-old, "Noah"), pragmatic, facing a real near-term decision (an actual electives form due)

**Verdict: no, not even Tier 1.** Deciding factor: the `middle-school-subject-selection-birth-chart` post that drew this parent in explicitly promises help "rank a coding elective against a second language" — the actual reading never mentions a second/foreign language at all, and rates Computer Science/Coding as "fairly typical... a gentle, playful introduction should work as well for them as for most children" (no clear signal either way).

Full findings, numbered as the persona gave them:
1. *"Bait-and-switch on the one thing that got me here."* The blog title names the exact use case; the actual reading has no elective-choice mechanism — "the same generic natural-strengths/subjects report you'd get for a 6-year-old, just relabeled with a 'Tween Years' header."
2. Internal contradiction — since fixed, §19 Bug 2: chapter 9 rated Computer Science/Coding "fairly typical," while chapter 11 ("Natural Direction") said "Electives in coding, robotics, or applied science are worth offering even before they ask," for the same chart. *"For $25 I want one clear answer, not two chapters disagreeing with each other."*
3. What genuinely worked: the trust/tone layer — "unusually honest for this category," no fear-mongering or fake credentials found, "the only reason I read past the homepage."
4. **The intake form doesn't capture the actual decision.** `/report` only asks for name/DOB/time/place — nowhere to say "the choice is coding vs. Spanish vs. extra art." *"So the product can't actually be personalized to my real-world form even if it wanted to; it's structurally generic by design."* This is a distinct, concrete, buildable idea — see action items below.
5. The one confident recommendation in the whole reading (Science, pushed hardest in chapters 6/14) isn't even an option on a typical electives form — "the one confident recommendation in the whole reading is for a subject I can't act on in two weeks."

**Age-fit verdict:** correctly states the child's age, has a "Tween Years" chapter, ties the dasha timeline to real calendar dates — but *"strip the 'Tween Years' header and this could be handed to a parent of a 7-year-old with zero edits to the substance."* Notes the blog post itself is honest that "the reading doesn't use different logic for a fourteen-year-old than a toddler" — accurate, but confirms the age-specific framing is cosmetic, not structural.

### Persona 3 — senior secondary (a 17-year-old, "Zara"), anxious, real stakes (university/subject decisions looming)

**Verdict: no, neither tier.** Deciding factor: the `senior-year-subjects-university-direction-birth-chart` post is specifically, thoughtfully about senior-year subject and university decisions — the actual reading, even the paid/premium version, is not. A generic child-development report with one "Zara is 17 now" paragraph inserted.

Findings:
- **A genuinely rare piece of honesty, called out by name as the best line on the site:** *"Be especially wary of any reading — ours or anyone else's — that speaks about a teenager's future in confident, specific, career-naming terms. That confidence is a red flag, not a feature."* The persona had braced for a "$40 destiny report" grift and found the opposite.
- The product doesn't match that promise: the Subjects chapters (9-10 of the premium reading) list Visual Arts, Mathematics, Music, Public Speaking, Reading/Writing, History, Science — *"a primary-school curriculum"* — nothing about actual senior electives (Methods vs. Specialist Maths, Chemistry vs. Biology, Economics, Legal Studies) or how a chart pattern maps to degree areas.
- Age-inappropriate advice, even after paying: *"Reading aloud together, even past the age it feels 'necessary'"* and *"a visible daily routine chart can turn structure into something reassuring"* — given about a 17-year-old. *"It made me trust the whole reading less... it signals the 'personalization' is a template with a name swapped in."*
- Sidebar reminders never change: every chapter, free or paid, repeats "try not to compare this report to a sibling's or classmate's" — irrelevant, tone-deaf framing for a senior applying to university.
- Genuinely good, when it shows up: Chapter 11 "Natural Direction" names concrete fields (Design, Architecture, Media & Film, Music, Marketing) and closes with *"Not a prediction, and not a shortlist — just a sense of the kind of work that tends to fit this pattern. What Zara actually chooses is entirely theirs."* Exactly the right register — but it's one chapter out of fourteen, buried behind the paywall, surrounded by content that undercuts it.

**Anxiety-handling verdict (the specific thing this persona was designed to test):** passed the "don't exploit fear" test cleanly — no predictions, no career mandates, real honesty throughout. Failed the "don't be uselessly generic" test: *"Being honest about limits is only reassuring if the thing underneath the honesty is actually about my problem. Here, the humility is real but it's wrapped around a report that's mostly generic child-development filler."*

**Age-fit verdict:** fails, mostly — the brand, nav CTA ("Get your child's reading"), intake form, and sidebar reminders never once adjust for a near-adult. Only one transition chapter acknowledges the child is 17 at all. "Natural Direction" is the sole chapter proving *"the team can write age-appropriately when they choose to — they just didn't do it consistently enough to justify the price for my actual situation."*

### Cross-cutting findings — what all three independently converged on

1. **Age-tailoring in the report is cosmetic, not structural.** Every persona, unprompted, described the same thing in different words: a chapter header or one inserted sentence changes ("Tween Years," "Zara is 17 now"), but the underlying subject/direction content is the same engine output regardless of the child's actual age. Confirmed directly in code before this test even ran (see the conversation history around this date): `src/lib/education/subjects.ts` and `direction.ts` have zero age-band branching.
2. **The new age-banded blog content (§17) now promises something the product doesn't structurally deliver.** This is a real, active expectation gap — a parent who clicks through from an age-specific post is set up to be let down in exactly the way all three personas describe.
3. **"Steady"/"typical"-tier content reads as anticlimactic for something paid.** An honest "about average" result is correct, but multiple personas independently found it a letdown when it's the majority of what a $25-35 purchase delivers.
4. **Static boilerplate that never varies (e.g. the sibling/classmate-comparison reminder) reads worse the older the child is.**
5. **The intake form has no way to capture the actual real-world decision a parent is facing** (which specific electives, which degree areas) — flagged concretely by the middle-school persona as the structural reason the product can't speak to a real choice even in principle.
6. **The trust/honesty layer is working exactly as intended and was praised by every persona** — the About/FAQ candor, the explicit "not a prediction" framing, the complete absence of fear-based or fake-urgency language. This is Phase 0's work (and the founder's original ethical stance, §6) paying off. **Do not touch this while fixing the above** — it's the reason any persona read past the homepage at all.

### Recommended next steps (not yet started — this is the actual task)

1. **Decide with the founder whether to invest in genuine age-band-aware report generation**, not just header/label changes. This is a real scope increase into the paid product's core engine (`subjects.ts`, `direction.ts`, `pathway.ts`), flagged as a decision point *before* the Phase 2 blog content shipped (see this session's conversation) — the persona tests now make the cost of not doing it concrete and specific rather than hypothetical.
2. **Consider an optional "what decision are you facing?" input** on the intake flow for older children (e.g. a free-text or short-list field: "what electives/subjects are you choosing between?") so subject/direction content can speak to the parent's actual real-world choice instead of staying abstract. This was the middle-school persona's single most concrete, buildable suggestion.
3. **Revisit "steady"/"typical"-tier copy** in `subjects.ts`/`metrics.ts` to see if it can carry more genuine substance without overstating a placement's strength — flagged independently by two personas as an anticlimax for paid content.
4. **Reconsider static boilerplate reminders** (e.g. the sibling/classmate-comparison line) for whether they should vary or be omitted for older age bands.
5. **Resolve the expectation gap** the age-banded blog posts (§17) now create — either build the product features to match what those posts promise, or soften the posts' promises to match current reality. Don't leave the mismatch as-is; it's actively costing trust once a real parent clicks through, per all three personas.
6. **Re-run this exact 3-persona conversion test after any of the above ships**, using the methodology above, before assuming a fix worked.

---

## 19. Two content-engine bugs found via the conversion test — both fixed

**Bug 1 — duplicate/reused citation text (commit `2940db2`).** The early-years persona (§18) found the identical citation sentence — citing the same Venus/Saturn/Mars placement — reused verbatim as "evidence" for four unrelated headline traits across different chapters. **Root cause:** in `src/lib/education/narrative.ts`, `renderTieredInsight`'s "extras" sentences (conjunction/dignity flavor text) had zero phrasing variants — one fixed sentence per planet, selected by a seed derived only from that planet's own chart data. Whenever two unrelated sections legitimately shared a lead planet (some by design, e.g. PE and Hands-On Direction both anchor to Mars; some by chart coincidence), their entire "extras" block rendered byte-identical. **Fix:** added real phrasing variants per planet, and replaced the seed-only lookup with a per-chart, per-`(leadPlanet, slot)` round-robin counter guaranteeing no repeat until variants are exhausted (a hash-based dedup approach was tried and discarded first — pigeonhole makes collisions mathematically unavoidable when 5+ sections can share one lead planet, e.g. Mercury across 3 subjects + 1 direction). Scoped to `narrative.ts` only — `scoring.ts` and the shared strength functions every other chapter depends on are untouched.

**Bug 2 — Subjects/Direction chapter contradiction (commit `2940db2`, same commit).** The middle-school persona (§18) found the Subjects chapter rate a child's Computer Science/Coding aptitude as "fairly typical" while the separate Natural Direction chapter, for the *same chart*, said "Electives in coding, robotics, or applied science are worth offering even before they ask." **Root cause:** the two chapters' underlying *scores* actually agreed (both "steady"/ordinary) — the contradiction came from `direction.ts`'s `stages` field being a single fixed string per stream, unlike every other tier-aware copy in this engine, so a "steady" or even "growing" stream still got confident, singled-out elective language. **Fix:** threaded the stream's strength tier into `stages` and tier-varied the "secondary/teen years" text for all four streams, so a "steady" rating now reads as "worth offering as one option among several" rather than a confident standalone recommendation.

**Architectural lesson, worth remembering for future work in this engine:** both bugs came from the same underlying pattern — content that's supposed to vary with a computed score/tier, but had a fixed, unvaried string somewhere in the pipeline. When adding any new chapter or insight type to `src/lib/education/`, check that every user-facing sentence actually threads through the tier/score, not just the citation.

---

## 20. Stripe preview checkout bug — a real database race, not what it first looked like (fixed)

**Status: root-caused and fixed, confirmed working end-to-end with a real test payment on preview.** Worth documenting the full chase, not just the fix — several plausible-looking causes turned out to be red herrings, and the actual bug is a genuine architectural gotcha that could recur elsewhere in this codebase.

**Symptom, as the founder tested it directly:** after a real (sandbox test-mode) payment on the preview branch, the redirect back from Stripe would land on the locked/free view again — as if payment hadn't worked. Clicking "Unlock" a second time skipped Stripe entirely (the existing `report.tier === tierId` short-circuit in `createCheckoutSessionAction` correctly detected the report was already paid) and landed straight on the genuinely unlocked reading.

**Red herrings ruled out, in the order they were investigated — useful if this class of bug ever resurfaces:**
1. *Stale Vercel deployment* — real issue encountered along the way (a "Redeploy" on an old deployment entry rebuilds that same old commit with new env vars, it does not pull the branch's latest commit), but not the root cause once ruled out via commit-hash checks in the Deployments tab.
2. *Missing `session_id` in the redirect URL* — checked directly, confirmed present every time.
3. *Stripe webhook signature mismatch on preview's Stripe Sandbox* — this one was real and is now fixed on the founder's side (Preview's `STRIPE_WEBHOOK_SECRET` in Vercel didn't match the Sandbox webhook endpoint's actual signing secret, confirmed via the exact "No signatures found matching the expected signature for payload" error in Stripe's own webhook event log). Worth knowing: a Stripe **Sandbox** is a fully separate environment from Live mode and needs its own webhook endpoint + secret registered independently — it does not share production's webhook config. This was a real, separate bug, fixed by the founder re-copying the correct signing secret and redeploying — but fixing it alone did not fix the reported symptom, which is what pointed at something deeper.
4. *Browser back-forward-cache (bfcache) restoring a stale pre-payment snapshot* — a reasonable-looking hypothesis given the symptom pattern (correct-looking server logs, wrong-looking rendered page), addressed defensively by forcing `Cache-Control: no-store` (`export const dynamic = "force-dynamic"` in `report/[id]/page.tsx`, kept in the final fix since it's still the right caching posture for a payment-state-dependent page) — but retesting after this shipped showed the identical symptom, ruling this out as the actual cause too.

**Actual root cause, confirmed via temporary diagnostic `console.log` statements (added, used, then removed — see commits `deb868a`/`027551d`) directly in Vercel's Function/Runtime Logs:** `markReportTier` would report success, but the very next `getReport(id)` call — in the same request, milliseconds later — could still return the pre-write tier (`null`). A genuine **read-after-write consistency gap on the Supabase/database side**, not a caching issue at any layer. The log evidence that nailed it: `justUnlockedTier: "full"` (from the successful write) alongside `reportTier: null` (from the very next read) in the same request's final state.

**The fix (commit `80a6ae5`):** `src/app/report/[id]/page.tsx` now computes `const effectiveTier = justUnlockedTier ?? report.tier;` and uses `effectiveTier` — not `report.tier` — everywhere the page decides what to render (whether to show the `PaymentConfirming` state, `unlockedPathway`/`unlockedRemedies`, and the `tier` prop passed to `ReportView`). The tier a request just wrote is known-good regardless of whether an immediate re-read reflects it yet.

**Other genuine improvements shipped in the same investigation (kept, not just diagnostic scaffolding):**
- `src/lib/stripe/server.ts`: `verifyCheckoutSession` retries the Stripe lookup a few times with a short delay before giving up (closes a separate, smaller timing window — Stripe can redirect a moment before a session's `payment_status` itself flips to "paid"), and reports a genuine API/config failure to Sentry instead of silently returning `null`.
- `src/app/report/[id]/PaymentConfirming.tsx` (new): when a parent returns from Stripe and neither the redirect-time check nor an existing tier confirms the unlock yet, this shows a quiet "Payment received — unlocking your reading..." state that polls `checkReportUnlockedAction` (new, in `report/[id]/actions.ts`) every 2.5s and refreshes once it lands, instead of showing the paywall again (which reads as "the payment didn't work"). After ~50s it points to the existing `/resend-reading` recovery flow.
- `src/app/api/stripe/webhook/route.ts`: a signature-verification failure is now reported to Sentry, not just returned in the HTTP response — this exact class of failure (§20 point 3 above) was previously invisible outside of manually checking Stripe's own dashboard.

**Confirmed by the founder:** a fresh test payment on preview landed directly on the unlocked reading with no second click needed. Interestingly, the `PaymentConfirming` message didn't even need to show — because the actual bug was fixed, the unlock now resolves within the same request, so the polling fallback simply wasn't needed. That's the correct outcome, not a miss.

**Note on production:** production's Stripe flow was independently re-verified working with a real purchase during this investigation, on the code *before* any of these commits existed there — the underlying race was never confirmed live on production. All of these commits have since been fast-forwarded to production regardless, since they're a genuine robustness improvement (the same race could in principle happen there too) and the fix carries no behavioral downside.

**Architectural lesson for future work anywhere in this codebase:** after writing to Supabase, don't assume an immediate subsequent read in the same request reflects that write. If a value is already known from the write itself, keep it in memory rather than re-fetching and trusting the re-fetch.

---

## 21. A dedicated "answer the parent's actual question" chapter, plus a real career-significator library (this session)

**Context this picks up from:** a live-preview test found that when a parent typed a real career question into the intake's decision-focus field (e.g. "will she get into medicine?"), the paid reading never actually answered it — it acknowledged the question, then talked about the child's strongest *subject* instead, which for a medicine question meant literally answering "her strong subject is mathematics." The founder's own words: *"this is a major disappointment for someone who has paid to just view the answer."* Building a real answer to that turned into a multi-step project: first a naive version, then founder-driven refinement once real testing exposed how shallow "one shared STEM score for every career" actually was, then three rounds of grounding the scoring in real classical source material once the founder supplied it. All of it is on the working branch `claude/handoff-task-review-umvc5i` (commits `796eb07` through `97232f2`), **not yet on production.**

**What shipped, in the order it was built:**

1. **`src/lib/education/directAnswer.ts` (new) — a dedicated "Your question, directly" chapter.** Matches the parent's free-text `decisionFocus` against tracked subjects (`decisionMatch.ts`'s `matchDecisionSubjects`) and career/field keywords (`matchDecisionCareers`), then gives a real, decisive-as-the-chart-actually-supports answer: a genuine head-to-head when two things are named, a single direct read when one is, and an honest "this chart doesn't lean either way — interest should decide" when the gap genuinely isn't meaningful. Never predicts admission/selection outcomes (grades/exams/effort own that, explicitly said every time) — only ever speaks to whether the *kind of work* suits the child's natural direction. Handles more than one named career in one question (e.g. "astronaut or aerospace engineer") without silently dropping one.

2. **`src/lib/astro/divisional.ts` (new) — the Dashamsha (D10) chart.** Career questions were being read from the natal (D1) chart alone; real practice reads D1 *and* D10, the divisional chart specifically for profession. Computes D10 by re-expressing the existing D1 planets under the standard odd/even-sign 3°-division rule, built as a genuine `BirthChart`-shaped object so all the existing dignity/house/aspect machinery in `scoring.ts`/`aspects.ts` runs against it completely unmodified. `careerSignals.ts` blends each field's D1 score with its D10 score (60/40). **Known, measured tradeoff:** a 60-random-chart stress test showed this drops the "confident lean" rate on Medicine-vs-Engineering-style comparisons from ~42% to ~25% — expected and correct (averaging two real, semi-independent classical checks should surface more genuine ties when they disagree, not manufacture more confidence), but worth knowing if revisited; the weight is a single constant (`D10_WEIGHT` in `careerSignals.ts`).

3. **`src/lib/education/careerSignals.ts` (new) — a real, source-grounded career-significator library.** Originally every STEM career (Medicine, Engineering, Computer Science, Applied Sciences, Architecture) shared one Mercury-heavy "STEM stream" score — the founder's own catch: *"medicine and mathematics is predicted for almost everyone."* Rebuilt against **three classical sources** the founder supplied (two PDFs, one pasted rules compilation — see below for where the PDFs live), each contributing genuine, cited corrections rather than a uniform rework:
   - *"Planets and Education Vol. I"* (Naval Singh, ed. K.N. Rao): the "technical vs. non-technical planet" framework (Sun/Mars/Saturn/Rahu/Ketu vs. Jupiter/Mercury/Venus/Moon), Ketu as medicine's named significator, Sun as mathematics/physics's, and distinct per-discipline engineering formulas (mechanical/civil/electrical/chemical each reading a different planet mix).
   - *"Encyclopedia of Vedic Astrology: Your Profession"* (Shankar Adawal): a direct planet→profession table and dedicated doctor/legal-profession chapters — added Rahu to Medicine and to Electrical Engineering specifically ("profession connected with electricity"), and rebalanced Law to lead with Mercury (the profession itself) rather than Jupiter (reserved for the judge's bench specifically).
   - A third, unattributed rules compilation (houses/signs/planets/divisional-chart method): added a genuinely new axis, a zodiac-sign affinity read from the natal 10th house's sign, applied only where that source names a field explicitly by sign (not stretched to fields it doesn't mention).
   Net result: 9 new career fields (Mechanical/Civil/Electrical & Electronics/Chemical Engineering, Microbiology, Biotechnology, Biochemistry, Mathematics & Statistics, Hotel Management), each reading its own weighted mix rather than a shared generic score.
   **Deliberately not implemented**, named in the file's own header comment for whoever picks this up: Jaimini's Atmakaraka-based technique (needs a new "which planet sits at the highest degree" primitive that doesn't exist yet) and Shad Bala/Vimshopak Bala (the real classical six-fold/twenty-point strength systems — meaningfully deeper than the `strengthScore()` proxy every chapter in this engine relies on, but each a substantial standalone project).

4. **Two real keyword-matching bugs found via testing, both fixed in `decisionMatch.ts`:** "mathematician" and "chemical engineer(ing)" were being swallowed by the Mathematics/Science *subject* keyword patterns (plain substring matches on "math"/"chem") before the *career* matcher ever ran, so `buildDirectAnswer` short-circuited on a single subject match and silently dropped a co-mentioned career — "doctor or mathematician?" answered only about the Mathematics subject and never addressed "doctor" at all. Fixed via negative-lookahead exclusions on both subject patterns.

5. **A real copy contradiction found via a live-preview screenshot, fixed in `narrative.ts` and `metrics.ts`:** a "steady"-tier free-tier metric explicitly labelled a strength ("one of the sturdier, more dependable areas") still read as a growth item, because its Saturn conjunction-flavor sentence said the quality "may take structure and consistency to fully mature" — a developmental-stage claim that fights whatever label sits next to it. Root cause: `narrative.ts`'s conjunction-flavor sentences render identically regardless of tier or (new, this session) strength/growth *role* — `metrics.ts` now appends a role-aware clarifying clause (`ROLE_CLAUSE`) so a "steady" placement reads differently depending on which bucket it's shown in, and three specific flavor sentences (Saturn, Jupiter, Ketu) that staked a developmental-stage claim were rewritten to describe the planet's character without claiming how far along the trait is. **This was a spot-fix on the exact instance a real screenshot caught, not a systematic audit — see §22, item 3.**

**Source material, for anyone continuing this work:** three PDFs were supplied over the course of this session and read via text extraction (not the chat-upload path — see the delivery-method discussion in this session's transcript if setting this up again). Two are currently sitting on branches other than production or this working branch and should be treated as reference material, not app code:
- `163400521-Planets-and-Education-vol1-Singh-KN-Rao-pdf.pdf` on `claude/handoff-context-review-8pf8w6`.
- `Encyclopedia_Of_Vedic_Astrology_Part1.pdf` / `Part2.pdf` were briefly and accidentally pushed to **production** (`claude/vedic-horoscope-learning-site-fb6fta`, commit `94f5513`) via the GitHub web-upload flow, then removed cleanly in a follow-up commit (`4a5efa7`) once caught — they were never referenced by app code, so removal was a plain deletion, no history rewrite, no risk. The extracted text is not saved anywhere in the repo; re-extract from the source PDFs if the content is needed again.
The third source (zodiac-sign/house/planet/divisional-chart rules) was pasted directly into the conversation, not supplied as a file — not saved anywhere outside this HANDOFF entry's summary of what was taken from it.

---

## 22. Content-layer consistency & repetition audit — the current task

**Why this is next:** §21 significantly deepened *how* career/subject scores are computed (Dashamsha, sign affinity, three sources' worth of real significators) without a matching pass over whether the *prose* built on top of all that scoring still reads as one coherent voice rather than a template with more moving parts. Two real defects surfaced already this session purely by accident — a live screenshot caught one contradiction (§21 item 5), a test question caught one silently-dropped career mention (§21 item 4) — which strongly suggests more of the same class exist unaudited, especially since §21 roughly doubled the number of career "fields" the content engine can talk about (8 STEM/humanities/arts/practical fields before, 25+ now) without proportionally growing the finite phrasing-variant pools those fields all draw on.

**Root causes identified, most important first:**

1. **The same underlying aptitude is sometimes scored two genuinely different ways in two different chapters, with nothing telling the reader that's deliberate.** Confirmed, concrete examples (verified by reading the actual formulas side by side):
   - Mathematics: the Subjects chapter's `mathematics` (`subjects.ts`) scores `Mercury + Saturn*0.5 + fifth-house-element bonus`. The direct-answer chapter's `Mathematics & Statistics` career field (`careerSignals.ts`) scores `Sun*0.5 + Mercury*0.4 + Ketu*0.3`, blended with its Dashamsha reading. These can disagree by more than one tier for the same chart — a parent reading "Mathematics: a genuine natural strength" in the Subjects chapter and then "being a mathematician... likely to need more deliberate encouragement" in the direct-answer chapter (or vice versa) has no way to know that's two different classical techniques being applied on purpose, not the site contradicting itself.
   - Same risk, not yet checked: the `science` subject (`Jupiter + Mercury*0.5 + mutable-ascendant bonus`, one generic score for chemistry/biology/physics together) versus the three new, much more specific career fields `Microbiology`/`Biotechnology`/`Biochemistry`, each reading a different Jupiter/Moon/Mars/Saturn/Rahu mix.
   - Same risk, not yet checked: `visual-arts` subject (`Venus` alone) versus the `Design (graphic, product, UX)` career field (`Venus*0.7 + Mercury*0.25`).
   - One pairing that happens to already be fine, worth knowing as a working example: the `music` subject and the `Music` career field in `careerSignals.ts` use the *identical* formula (`Venus*0.5 + Moon*0.5`) — but that's coincidental, not by design, so it doesn't prove the pattern is safe elsewhere.
   
   **Two ways to close this, not mutually exclusive — pick per case:**
   - Make the career-field formula a genuine superset of the subject's formula (reuse its core terms, add career-specific terms on top) wherever they're meant to represent the same underlying thing, so they can't wildly diverge.
   - Where the divergence is intentional (career-level analysis deliberately draws on Dashamsha/sign-affinity that a school-subject score doesn't), say so briefly in the direct-answer copy when the two reads meaningfully differ — turn a looks-like-a-contradiction into visible, deliberate depth instead.

2. **No audit exists, at the current content scale, for either failure mode.** Everything found so far (§19's two bugs, §21's two bugs) was caught by a human reading a specific generated report, not by a repeatable check. Before doing anything else, build one: generate full reports for a decent sample of charts (~50-100, varying birth date/time/location the way earlier stress tests in this session did), and for each report check (a) no sentence appears twice verbatim within one report, and (b) for every subject/career-field pair that names the same real-world concept (the four pairs above, plus any others found while doing this), the two tiers either agree or the copy visibly acknowledges why they don't. This can live as a throwaway dev script (see this session's transcript for the `tsx`-based pattern used to stress-test `directAnswer.ts`) rather than a committed test suite, unless the codebase already has a test runner worth wiring into — check first.

3. **The tone-vs-label defect class from §21 item 5 was only spot-fixed, not audited.** `narrative.ts`'s `CONJUNCTION_FLAVORS` has 9 planets × 6 variants = 54 sentences; only 3 were checked (and fixed) because one specific chart's output happened to surface a bad one in a screenshot. Systematically read all 54, plus `EXALTATION_INTENSIFIERS` and `CITATION_TEMPLATES`, for any sentence that stakes a claim about developmental stage — words like "not yet," "already," "one of the more defining traits," "needs to mature," "less than expected." Extend the same check to `careerSignals.ts`'s `FIELD_ESSENCE`/`TIER_BLURB`/`DIRECTION_TIER_BLURB` text and `metrics.ts`'s new `ROLE_CLAUSE`, all added or touched this session and not yet stress-tested for this specific failure mode the way the original narrative.ts pools now have been (partially).

4. **The repetition "practical ceiling" (see §19's architectural lesson) was tuned against a smaller content surface and hasn't been re-measured since §21 roughly tripled the number of career fields a direct-answer body can cite a placement for.** Re-run the duplicate-sentence stress test (this session's methodology: `computeBirthChart` + `buildEducationInsights`/`buildDirectAnswer` over ~60 random charts, count exact-duplicate sentences per report) before deciding whether `CONJUNCTION_FLAVORS`/`CITATION_TEMPLATES` need more variants than the current 5-6 per planet.

5. **A genuine opportunity, not a defect:** the Dashamsha blend and sign-affinity bonus in `careerSignals.ts` are currently silent contributors to a number — the direct-answer copy names "Dashamsha" generically but never says, e.g., "your 10th house falls in Scorpio, a sign classically tied to engineering and surgery" on the specific charts where that bonus actually fired. Surfacing this selectively (only when a technique's contribution was large enough to matter, not appended to every answer) would make the depth investment from §21 actually visible to a paying parent reading the result, not just present in the math behind it.

**Suggested order of work:** item 2 (build the audit) before anything else — it turns items 1, 3, and 4 from "read the code and hope" into "run the script and get a list." Item 5 is a nice-to-have once the audit is clean, not a blocker.

---

## 23. §22's audit ran — three real findings, all fixed; one residual quantified, one opportunity still open

**Status: done, on the same working branch as §21/§22, not yet merged/pushed to production.** This session followed §22's own suggested order: built the audit first (item 2), then used it to turn items 1, 3, and 4 into concrete, numbered findings rather than a re-read of the code.

**The audit script.** A throwaway `tsx` script (per HANDOFF §22 item 2's own instructions — no test runner exists in this repo, checked via `package.json` first) that:
- Generates 80 charts across randomized birth dates (1990-2025), times, and latitudes/longitudes spanning both hemispheres.
- Runs each chart through the *actual* report-building functions — `buildEducationInsights`, `buildSubjectGuidance`, `buildFutureDirection`, `buildDirectAnswer` — across all five age bands and eight representative `decisionFocus` strings (including the exact phrasings from §21's own worked examples: "doctor or mathematician?", "coding vs a second language," etc.), not synthetic text.
- Check (a): splits every rendered body into sentences and flags any sentence appearing more than once within one report.
- Check (b): for each of the concrete pairs §22 named (Mathematics, Science↔Microbiology/Biotechnology/Biochemistry, Visual Arts↔Design, plus Music as the known-fine control), computes both the subject's tier and the career field's tier directly from their real scoring functions and flags a mismatch.
- Check (c): scans every rendered sentence for a set of regex patterns matching developmental-stage language ("at first," "not yet," "plateau," "shows up early," "doesn't need much encouragement," etc.), then groups by sentence text to show which tiers/roles each one actually co-occurs with — a sentence appearing only under one tier is normal (that tier's own copy); a sentence appearing under multiple tiers is the tone-vs-tier contradiction §21 item 5 first caught.
- Deleted before committing, per §9's established convention (no test runner exists to wire it into, and HANDOFF §22 itself sanctioned throwaway). The methodology above is preserved here in full so a future agent can reconstitute it in a few minutes rather than reinventing it — this is the second time this exact ask ("re-run before deciding") has come up (§19's architectural lesson, §22 item 4), so it's worth having the recipe on hand rather than rediscovering it a third time.

**Finding 1 (§22 item 2's check (a), confirmed and fixed) — `ROLE_CLAUSE` in `metrics.ts` had zero phrasing variants and was rendering as a byte-identical duplicate in 96% of the 80-chart sample (77/80 charts, 155 duplicate instances).** Root cause: `ROLE_CLAUSE` (added in §21 item 5's fix, to disambiguate whether a "steady"-tier metric was being shown as a strength or a growth area) was a single fixed string per `(role, tier)` pair — the exact same class of bug §19's fix already eliminated everywhere else in this engine (citations, conjunction flavor text), just never applied to this pool because it was added afterward. **Fix (commit on this branch):** `ROLE_CLAUSE` now holds real phrasing variants — 4 for each `strength` tier (matching the 4 possible "strengths" slots per report, `ranked.slice(0, 4)` in `engine.ts`, so round-robin selection *guarantees* zero collision even in the worst case) and 2 for each `growth` tier (matching the exactly 2 "growth" slots, `ranked.slice(-2)`), selected via the same per-chart round-robin counter (`nextFlavorIndex`, now exported from `narrative.ts`) already used everywhere else. Re-running the audit after the fix: down to 51/80 charts, 92 instances, then down further to 18/80 charts, 35 instances after the variant-count fix above — see Finding 3 for why a small residual remains.

**Finding 2 (§22 item 3, confirmed and fixed) — a systemic version of the exact bug §21 item 5 had already spot-fixed once.** `narrative.ts`'s `EXALTATION_INTENSIFIERS` (a sentence pool acknowledging when a section's lead planet is exalted — "worth watching for early," "rarely stays subtle") was firing purely off the lead planet's own dignity, with no check on the section's *overall* computed tier. Since a section's score is usually a blend of several planets/houses (e.g. `mathematics = Mercury + Saturn*0.5 + bonus`), a chart where the lead planet is genuinely exalted but another term in the formula drags the total into "steady" would get "fairly typical" framing immediately followed by "unusually pronounced... rarely stays subtle" — a direct contradiction, found in ~27-30 of the 80 sample charts (appearing under both `flourishing` and `steady` tiers before the fix). Systematically reading the rest of the pools this finding lives in, per §22 item 3's full ask: `CONJUNCTION_FLAVORS` also had two surviving instances of the same *class* of bug (staged/developmental-stage claims independent of tier) that the original §21 item 5 fix had missed — a Saturn variant ("slower to show at first... built to last once it does") and a Jupiter variant ("grow into it rather than plateau early"), both confirmed by the audit rendering across all three tiers. `CITATION_TEMPLATES`, `HOUSE_LORD_TEMPLATES`, `TIER_BLURB`/`DIRECTION_TIER_BLURB` (`directAnswer.ts`), and `FIELD_ESSENCE` (`careerSignals.ts`) were all read in full and are clean of this defect class — the first two are pure dignity/placement phrasing with no life-stage language, and the latter two are either indexed exactly by the tier they describe (self-consistent by construction) or are static, tier-independent factual descriptions. **Fix:** `dignityIntensifier` now takes the section's `tier` and only renders for `"flourishing"` (every one of its sentences stakes a strength claim, unlike `conjunctionFlavor`'s pool, which describes a planet's character and is meant to render regardless of tier — so gating is right for one pool and wrong for the other). The two `CONJUNCTION_FLAVORS` sentences were rewritten to describe Saturn's and Jupiter's character without staking a developmental-stage claim, matching the approach the original §21 fix used for its three sentences.

**Finding 3 (§22 item 4, quantified, not fully eliminated) — the repetition "practical ceiling" was re-measured, and a real but smaller residual remains.** After Findings 1 and 2's fixes, the audit still finds 18/80 charts (22.5%) with one or two duplicate sentences, concentrated in "stellium" charts where several unrelated sections (different lead planets, coincidentally all conjunct or ruled by the same partner planet) each call `conjunctionFlavor` or a citation function for that same partner/planet enough times to exhaust its 5-6 variants and wrap back to a repeat — the same pigeonhole mechanics `narrative.ts`'s own comments already describe, just occasionally still exceeded at the current variant counts now that §21 added more chapters (and more calls) that can share a conjunction. This is real, quantified, and worth fixing, but it's a much smaller problem than Findings 1-2 were (a handful of charts, one or two repeated sentences each, vs. 96% of reports before) and would mean either substantially growing every `CONJUNCTION_FLAVORS`/`CITATION_TEMPLATES` pool (a bigger, slower undertaking — each needs genuinely distinct phrasing, not filler) or threading a cross-chapter dedup context, which `narrative.ts`'s own comments already flagged as "a materially bigger, riskier change" when this same tradeoff was weighed for a different fix. Left as-is for now — not blocking, but worth another pass if it starts showing up in real founder/persona testing.

**Finding 4 (§22 item 1, confirmed, fixed via acknowledgment not formula-merging) — subject-vs-career-field divergence is real and now visibly acknowledged.** The audit confirmed real, meaningful tier divergence for every pair §22 named: Mathematics (27/80 charts, 34%), Science↔Microbiology (28/80, 35%), Science↔Biotechnology (25/80, 31%), Science↔Biochemistry (26/80, 33%), Visual Arts↔Design (18/80, 23%) — versus Music (8/80, 10%), the pair §22 flagged as coincidentally already using an identical formula, which the audit confirms is meaningfully rarer. **Fix (deliberately the "acknowledge, don't merge" option §22 itself offered):** merging the career-field formulas into the subject formulas was ruled out — §21's formulas are each individually grounded in specific citations from three classical sources (see §21's own file-header comment in `careerSignals.ts`), and forcing them into a shared base would mean either diluting that grounding or fabricating a rationale for a shared term the sources don't actually support. Instead, `directAnswer.ts` now carries a `FIELD_TO_SUBJECT` map (the same five pairs above) and, whenever a career-field answer's tier genuinely differs from its counterpart subject's tier, appends a brief acknowledgment: which tier the Subjects chapter gave that concept on its own, framed as "not a contradiction, just a narrower read," and naming *why* the career answer differs (its own significators plus the Dashamsha, not the general subject placement alone). Verified directly (not just via the audit) against a real generated `directAnswer` body for "doctor or mathematician?" — see this session's transcript for the exact rendered text. Applied to all three `buildDirectAnswer` branches that can name one of these five fields (single-field, and both multi-field comparison branches).

**Finding 5 (§22 item 5) — not attempted this session, still open, still a nice-to-have not a blocker,** exactly as §22 itself scoped it. Surfacing *which* technique (Dashamsha blend vs. sign affinity) actually swung a specific answer remains real, valuable, unbuilt work for whoever picks this up next.

**What's actually left, in priority order, for whoever picks this up:**
1. Nothing in §22's audit is a launch blocker anymore — Findings 1, 2, and 4 (the three defects that would have read as the site contradicting itself to a paying parent) are fixed and verified.
2. Finding 3's residual repetition ceiling is real but small (22.5% of a random sample, 1-2 sentences each) — worth a pass if it surfaces in real testing, not urgent on its own.
3. Finding 5 (surfacing which classical technique drove a specific answer) is the one genuine content *opportunity* left on the table from §21's investment — still unbuilt.
4. **This branch (`claude/handoff-task-review-umvc5i` / `claude/handoff-context-verify-xjd179`) still hasn't been merged to production** (`claude/vedic-horoscope-learning-site-fb6fta`) — everything in §21, §22, and this section exists only here. Re-run the §7/§18-style persona conversion test once this does ship, since the direct-answer chapter's copy has changed materially since the last such test.
5. Build+lint were run clean before this section was written (`npm run build`, `npm run lint`) — both pass with no errors introduced by this session's changes.

---

## 24. Two rounds of direct founder feedback on the live "Your question, directly" and Subjects chapters — both fixed

**Status: done, same working branch, not yet on production.** The founder screenshotted the actual rendered app (a `directAnswer` fallback response and a Subjects "support" card) rather than describing the issue abstractly — worth noting because both turned out to be real, specific bugs a description alone might not have pinned down precisely.

**Feedback 1 — disclaimer-first ordering in `directAnswer.ts`.** The founder's screenshot showed the "nothing recognized" fallback branch (a decisionFocus like "Suggest the course zara can opt in university" that matches no tracked subject or career keyword): the real content — "Physical Education & Sports stands out as a genuine natural strength" — landed at the very end, after a longer disclaimer about what the chart *can't* answer, closing on "It's not a direct answer to what you asked." The founder's read: this sets a negative tone by leading with what the product can't do. On inspection, the single-career-field branch (`reads.length === 1` — the actual "will she get into medicine?" case, likely the most common real use of this chapter) had the identical structural problem: "Whether X is ultimately admitted... not something a birth chart can predict" led, with the real tier read buried in the middle. Both rewritten to lead with the real, chart-grounded answer, then close with a brief, gentle "worth being clear about what that is, and isn't" clause covering the same honesty constraint (§6) without opening on it. The other four `buildDirectAnswer` branches (two-subject comparison, multi-field comparison) already led with the answer and were left alone.

**Feedback 2 — a real per-band content gap in `subjects.ts`'s `reading-language` subject, not just a wording nitpick.** The founder's second screenshot showed the "growing"-tier support card for an older child (Zara, in the middle-school-or-older range going by the surrounding "university" question) with a tip reading "Let them talk an idea through out loud first, even record it, before writing it down" — flagged as sounding written for a primary-aged child. Checking the code confirmed why: `reading-language` already had a `forSeniorBands` tip override (senior/youngAdult get "Talking an idea through out loud first, or using voice-to-text...") but, unlike `mathematics`/`science`/`computer-science` (which all have both a `middle` override *and* a senior one), it had no `middle`-band override at all — so an 11-13-year-old fell straight back to the early-years base tip with no age-appropriate stop in between, which is exactly the tier a tween is likely to land in. Added a `middle`-band tip override (flourishing/steady/growing) in the same tween "real audience, real choice" register `mathematics`'s existing middle override already uses, distinct from both the early-years base tip and the senior tip's university-application framing. Only `reading-language` was touched — this session didn't audit whether `visual-arts`, `music`, `history-social`, `public-speaking`, or `physical-education` have the same gap (they also currently only have `forSeniorBands`, no `middle`, tip overrides), since the founder's feedback was specific to this one subject; worth a look if the same complaint surfaces there.

**Verification:** both fixes were checked directly against generated output (not just read in code) — the reordered `directAnswer` bodies for the exact fallback and medicine-career cases from the founder's screenshots, and the `reading-language` tip across primary/middle/senior bands on charts where that subject actually renders. `npm run build` and `npm run lint` both clean.

**Follow-up, same day: the founder asked for the `reading-language` middle-band fix to be extended to every subject, for all three age groupings.** Auditing all 9 `SUBJECTS` in `subjects.ts` found the gap flagged in Feedback 2 was real and widespread, not a one-off: `history-social`, `visual-arts`, `music`, `public-speaking`, and `physical-education` had a senior-band tip override but *no* middle-band one at all (falling straight back to early-years-flavoured base copy for any 11-13-year-old); `mathematics` and `science` had a middle override for the `flourishing` tier only, leaving `steady`/`growing` (arguably the tiers where age-appropriate phrasing matters most, since that's where a concrete "how to help" tip actually gets used) on the base copy; `public-speaking` was additionally missing a `steady` tip for the *senior* band. Added a full three-tier `middle`-band tip override to every subject that lacked one, in the same tween "real elective/real choice starting to matter" register `mathematics`'s and `computer-science`'s existing middle overrides use, and filled the two gaps above. Verified with a small script asserting, for all 9 subjects × 3 tiers, that a tip is defined for every age band and that the young/middle/senior groupings are never all three identical (i.e. every subject now genuinely varies by age, not just some) — 0 missing, 0 identical-across-all-three, confirmed clean. `npm run build`/`npm run lint` both clean.

---

## 25. §21-§24 merged into production (1 Sep 2026)

**Status: live.** At the founder's explicit request ("push it into production"), the working branch (`claude/handoff-task-review-umvc5i`, continued this session as `claude/handoff-context-verify-xjd179`) was merged into `claude/vedic-horoscope-learning-site-fb6fta` and pushed — merge commit `5fe927d`, fast-forwarding production from `4a5efa7`. Before pushing, the founder was told explicitly what this brought in — not just this session's copy fixes, but all 14 commits of accumulated §21-§24 work (the new career-significator library, the Dashamsha/D10 chart, the new "answer your question directly" chapter, and the content-consistency audit fixes) — and confirmed merging all of it now, over the alternative of cherry-picking just this session's fixes.

**What actually shipped, concretely:**
- `src/lib/education/careerSignals.ts` (new), `src/lib/astro/divisional.ts` (new), `src/lib/education/directAnswer.ts` (new) — the full §21 feature set: a from-scratch, three-source-grounded career-significator library (9 new career fields), a Dashamsha (D10) divisional chart used specifically for career reads, and a dedicated chapter that directly answers whatever the parent typed into the "what decision are you facing?" field.
- All of §22/§23's content-consistency fixes (duplicate-sentence fixes in `metrics.ts`/`narrative.ts`, the `EXALTATION_INTENSIFIERS` tone-vs-tier gating fix, the subject-vs-career-field divergence acknowledgment in `directAnswer.ts`).
- §24's founder-feedback fixes (lead-with-the-answer reordering in `directAnswer.ts`, and the full nine-subject, three-age-band tip pass in `subjects.ts`).
- `git log` and a clean `npm run build` + `npm run lint` were both verified against the actual merged state (not just the pre-merge branch) before pushing — the merge itself had zero conflicts.

**What genuinely wasn't done, and is worth being honest about:** the persona-conversion-test re-run that §21's own HANDOFF entry (§23, §24) flagged as the right check before this ships — "re-run this exact 3-persona conversion test... before assuming a fix worked," and "re-run the §7/§18-style persona conversion test once this does ship" — has **not** happened. This was a direct, explicit founder instruction to ship now, not a judgment call that skipping QA was fine; the founder was told what was being merged (see above) before confirming. But a future agent shouldn't read "it's live" as "it's been re-validated the way this project's own process calls for" — those are different things. **Recommended next step for whoever picks this up: run the §18-style 3-persona conversion test against production**, specifically checking whether the new direct-answer chapter and the deeper career-significator scoring hold up under a real skeptical/pragmatic/anxious-parent read, the same way the original age-tailoring work was validated.

**One structural thing worth flagging for future sessions:** this repo's `AGENTS.md`/`CLAUDE.md` says HANDOFF's own §9 working convention treats `claude/vedic-horoscope-learning-site-fb6fta` as "this repo's only branch and its default/HEAD branch... no merge/PR step is needed." That was true when written; it no longer is — `claude/handoff-task-review-umvc5i`/`claude/handoff-context-verify-xjd179` has now existed as a real, multi-session working branch for several days' worth of work, merged in explicitly rather than developed directly on production. Whether future work should default back to committing straight to production, or continue using a working branch and merging deliberately (as this session did), is worth the founder's explicit call rather than assumed either way.

---

## 26. Persona conversion-test re-run (1 Sep 2026) — four real findings, all fixed; one false alarm, corrected

**Status: done, on this working branch, not yet re-merged to production** (§25's merge happened *before* this re-test; these fixes are new, on top of it). At the founder's request, re-ran the §18-style 3-persona conversion test §25 flagged as still owed — same personas (Ivy 4, Noah 12, Zara 17), same "genuinely decide whether to pay, no source-code reading" methodology, but now specifically probing whether §21-§24's changes (the direct-answer chapter, the deeper career scoring, the content-consistency fixes) hold up under a real black-box read.

**Methodology, reconstructed from §18's notes since the temp route isn't preserved between sessions:** a throwaway `src/app/dev-preview/persona/[id]/page.tsx` (never committed, deleted after this session — same convention as before) rendered `ReportView` for three fixed children via the real `computeBirthChart`/`buildEducationInsights`/`buildLearningPathway` pipeline, at both `tier=null` (locked/free) and `tier=premium`. Each of the three personas was a separate agent, browsing `http://localhost:3000` (not the live domain — this sandbox blocks that) via Playwright (`npm install -D playwright`, then uninstalled after, per §9), with explicit instructions not to read source code and not to actually submit a payment. Noah was given the decision "coding vs a second language for electives" (the same scenario §18 originally found broken) and Zara "torn between medicine and psychology for uni" (new, to exercise the multi-field career comparison §21 added) — Ivy had no decision, matching her original early-years persona.

**Verdicts:** Ivy's parent — pay $25, not $35. Noah's parent — would pay $35 for the direct-answer chapter alone, but see the false alarm below. Zara's parent — pay $25, not $35; found Chapter 8 honest but "a tie" wasn't actually useful for her real decision, and specifically praised that it no longer buries the answer under disclaimers (§24's fix confirmed working from a fresh, independent read).

**Finding 1 (confirmed, fixed) — the stellium duplicate-sentence problem was real, not just a theoretical residual.** §23 Finding 3 had already quantified and consciously left this as a known, lower-priority gap (~22% of a random sample). This re-test made it concrete: Ivy's persona and Zara's persona *independently* quoted the exact same class of defect — a `CONJUNCTION_FLAVORS` sentence ("Rahu's presence gives this an unconventional, intensely focused edge...") appearing byte-for-byte identical across two or three unrelated chapters (e.g. describing both "core intelligence" and, separately, "History & Social Studies"). Both charts happened to have several planets conjunct in one house (a real "stellium"), which calls `conjunctionFlavor` for the same partner planet far more times in one report than that planet has phrasing variants (6) — the existing round-robin counter (`nextFlavorIndex`) wraps around once exhausted, so past the 6th call it starts repeating from the top. **Fix:** added `nextFlavorIndexOnce` (`narrative.ts`) — same counter, but returns `null` once a pool is exhausted instead of wrapping around; `conjunctionFlavor` and `dignityIntensifier` now omit the flourish sentence entirely past that point rather than repeat one, falling back to just the (always-present) citation clause. A stellium chart's report is very slightly shorter in a few spots as a result — a better trade than a visible repeat. Re-ran the §22/§23 audit methodology (80 random charts): duplicate instances dropped from 35 to 3, charts affected from 18/80 to 3/80 — and the 3 remaining are citation-clause repeats (a lower-severity, structurally-required text neither persona actually flagged), not the flavor-sentence repeats either persona quoted.

**Finding 2 (confirmed, fixed) — classical yogas (Special Combinations) and the graded strength scores elsewhere can describe the same planets in ways that read as a flat contradiction, with nothing telling the reader that's expected.** Ivy's persona caught this precisely: her chart's Mercury-Jupiter-Sun cluster is cited by `yogas.ts` as "Saraswati Yoga... real natural aptitude for learning" and "Budha-Aditya Yoga... sharp, analytical thinking" in the Special Combinations chapter, while the *same* planets are described in the Areas to Nurture chapter (via `metrics.ts`'s house9/mercury scores) as "typical, unremarkable... more room to grow." Both reads are individually correct — yogas are a fixed, named-alignment technique, metrics are a separate graded score, and classical astrology treats them as genuinely distinct lenses — but nothing in the copy said so, so a careful parent reads it as the site contradicting itself. Same root-cause class as §22 Finding 4 (subject vs. career-field divergence), just a different pair. **Fix:** added one clarifying sentence to the Special Combinations chapter's intro (`ReportView.tsx`): "These read a fixed, named alignment, not the graded strength score used elsewhere in this reading... Not a contradiction, just a different, older lens on the same chart."

**Finding 3 (confirmed, fixed) — the direct-answer chapter, arguably the single strongest reason a parent with a specific question would pay, isn't mentioned anywhere a purchase decision actually gets made.** Found independently by *both* Noah's and Zara's personas: neither the pricing-tier feature bullets (`pricing.ts`, rendered on the paywall `TierCard`) nor the FAQ's tier-breakdown answer mention that a "Guiding Stars" purchase includes a real, direct answer to whatever decision the parent named. Both personas said they only found out it existed because they were told to look for it. **Fix:** added "A direct answer to the real decision you're facing, if you tell us what it is" as the lead bullet in the `full` tier's `features` list (`pricing.ts` — this is a `full`-tier feature, not premium-exclusive, since `buildDirectAnswer` runs for any purchased tier), and folded the same into the FAQ's tier-comparison answer (`src/app/faq/page.tsx`).

**Finding 4 (confirmed, fixed) — the direct-answer chapter can call a field "a solid fit" while the separate Natural Direction chapter never mentions it at all, with no explanation.** Zara's persona caught this on her actual test case: Chapter 8 said both Medicine and Psychology were "a solid, workable fit," but Chapter 11 (Natural Direction) — built from `direction.ts`, which only ever surfaces the single strongest of four broad streams — named only creative/hands-on fields, because that happened to be her chart's strongest stream. Neither field she asked about was even eligible to appear in Chapter 11. **Fix:** `directAnswer.ts`'s `fieldRead` already computed `isPrimaryStream` but only used it for the *positive* case (a congratulatory note when the field *does* match). Added the negative case too: when a named field's parent stream isn't the child's primary direction (and the field's own tier isn't `growing`, i.e. there's a real signal worth explaining), a brief note now says the Natural Direction chapter will point elsewhere and why that's not a disagreement. Wired into both the single-field branch and (a gap the first pass missed, since Zara's actual test case — "medicine vs. psychology" — is a *two*-field comparison, a different code path) the two multi-field branches via a new `directionGapNote` helper.

**One finding investigated and found to be a false alarm — worth recording so it isn't "fixed" again by mistake.** Noah's persona reported, as their single most damaging finding, that the real `/report` intake form has no field at all for a parent's decision/question — meaning the direct-answer chapter (which the blog post promises) is unreachable by a real customer. Checked directly against `ReportFlow.tsx`: the `decisionFocus` field is real and correctly wired — it's conditionally rendered (`showDecisionFocus`) only once a valid DOB is entered and the resulting age band is `middle`/`senior`/`youngAdult`, exactly matching `directAnswer.ts`'s `DECISION_AWARE_BANDS`. Verified live with a direct Playwright script: the field is absent before a DOB is filled in and appears correctly after entering a 12-year-old's DOB. Zara's persona (independently) found and used this same field correctly ("once I entered a teen birth date, a new field appeared"). Noah's persona's claim was a testing-methodology gap — almost certainly inspecting the form's markup before ever interacting with it, which a client-side-conditional field won't show — not a real product defect. No code change made for this one.

**Verification:** all four fixes checked directly against the dev-preview routes for the actual charts the personas flagged (not just re-read in code) — the yogas framing sentence on Ivy's chart, the pricing/FAQ copy via `curl`, and the `directionGapNote` specifically against Zara's own "medicine vs psychology" scenario (confirmed only after realizing the first pass had wired it into the wrong branch). `npm run build` and `npm run lint` both clean; the `.next` build cache had to be cleared once after deleting the temp route (a stale generated type-validator file referenced the deleted page — a build-cache artifact, not a source issue).

**What's still open:** the citation-clause duplication residual (3/80 charts in the re-run audit) — lower severity, not what either persona actually flagged, left as-is. §22 item 5 (surfacing which classical technique — Dashamsha vs. sign-affinity — drove a specific answer) remains unbuilt, still a nice-to-have. **This branch has not been pushed to production since these fixes were made** — the founder should decide whether/when to re-merge, the same way §25's merge was an explicit, informed decision rather than an assumed one.

---

## 27. Gifting overhaul, discount codes, purchase email, and a Career Deep-Dive chapter (1 Sep 2026)

**Status: done, on this working branch, not yet merged to production.** Four founder requests, planned together (via plan mode, approved before implementation) since they all touch the purchase path of a real, live paid product. Two decisions were made explicitly with the founder before building: discount codes use Stripe's own native Promotion Codes (no new table, dashboard-managed) rather than a custom system; the gift voucher is a full deferred-redemption system (recipient enters their own child's details later), not just "email someone the finished reading."

**1. Every paid purchase now emails the reading link.** New `src/lib/email/readingEmail.ts` — a warm, on-brand HTML template (inline styles, since email clients don't run Tailwind) with a plain-text fallback, matching the site's own voice rather than a generic SaaS-receipt look. Wired into the webhook's `checkout.session.completed` handler, addressed to the payer's Stripe-captured email. This didn't exist before — a purchase's only prior confirmation was the redirect back to the unlocked report page; the "resend my reading" flow (§10) sent a bare list-of-links email, and only on request.

**2. Gift-delivery at the paywall, not the free intake form.** The existing `isGift` checkbox (on `/report`'s free intake form) only ever changed the reading's own cosmetic framing text — nothing functional happened when checked, which is what the founder flagged. Rather than repurpose that checkbox, a new "Send this as a gift instead" toggle was added to `TierCard` in `ReportView.tsx` — at the point of purchase, since a chart already exists by then. Checking it reveals recipient email (required)/name/note fields; these thread through as Stripe metadata (`recipientEmail`, `recipientName`, `giftNote`) via `createCheckoutSessionAction`, and the webhook sends a gift-framed email (`sendGiftReadingEmail`) to the recipient in addition to the buyer's own receipt copy. The original intake-form `isGift` checkbox is untouched — it still does what it always did (cosmetic framing), which was working and wasn't part of the complaint.

**3. A real gift-voucher subsystem, for when the buyer doesn't have the recipient's birth details.** This is a genuinely new purchase path, not a variant of the existing one — there's no chart to attach a checkout to. New `gift_vouchers` table (`supabase/migrations/0004_add_gift_vouchers.sql` — **must be run manually against the live Supabase database before this ships, same lesson as §10's incident; migration files in this repo do not auto-apply on deploy**), new `src/lib/giftVouchers/store.ts` (create-pending → webhook marks paid + generates an 8-char human-typeable code → redemption claims it exactly once, idempotent against a double-submit). New `/gift` page (tier + recipient email/name/note, no birth details) and its own Stripe Checkout entry point (`src/app/gift/actions.ts`, metadata `kind: "giftVoucher"` instead of a `reportId`) that the webhook branches on before the existing report-tier logic. New `/redeem/[code]` page + action: the recipient enters their own child's birth details, the report is created and immediately unlocked at the voucher's tier (no second Stripe charge — `markReportTier` is called with a synthetic `giftVoucher:<code>` session id for provenance), and the voucher is marked redeemed. `/terms`'s "Gift purchases" section now covers both delivery modes.

**Avoiding the two-similar-paths-drift risk this project has been bitten by before (§19's architectural lesson):** the free intake form (`/report`) and the redemption form (`/redeem/[code]`) both collect the same child birth details and must compute the same chart/report content from them. Rather than duplicate that logic, it's factored into `src/lib/reports/buildReport.ts` — one shared `birthDetailsSchema` (Zod) and one `computeReportPayload(data, isGift)` function, used by both `src/app/actions.ts`'s `generateReportAction` and `src/app/redeem/[code]/actions.ts`'s `redeemGiftVoucherAction`. `src/app/actions.ts` was refactored to use it (net simplification, not just a passthrough).

**4. Stripe Promotion Codes.** `allow_promotion_codes: true` added to both checkout-session-creation calls (the existing report checkout and the new gift-voucher checkout). Stripe's own hosted Checkout page then shows a native "Add promotion code" field; the founder creates/manages codes directly in the Stripe Dashboard (Product catalog → Coupons/Promotion codes) — no admin UI, no new table, no custom validation to build or maintain. Worth being precise about the distinction the founder's own phrasing blurred: this is a *discount* code (percent/amount off, applied by the payer at their own checkout, validated by Stripe) — a different thing from a *gift voucher* code (§27.3 — a single-use credit someone else already paid for, validated by this app's own `/redeem` page). The two are unrelated code namespaces on purpose.

**5. Career Deep-Dive — a new premium-tier chapter, approved by the founder as part of this batch (not just a recommendation).** The $15 full→premium upgrade previously unlocked exactly one chapter (remedies) — thin for the price, per the founder's own complaint. New `src/lib/education/careerDeepDive.ts`: ranks every career field `careerSignals.ts` actually scores (25+ fields across all four `direction.ts` streams, not just the single strongest broad stream the existing Direction chapter shows) and surfaces the top 6 with a tier-appropriate blurb — no new astrological computation, this is a presentation layer over data the engine already computes for `directAnswer.ts`'s career-question path. Computed once at report-creation time (`computeReportPayload`, same "compute now, gate by tier at display time" pattern as remedies) and stored (`career_deep_dive` jsonb column, `supabase/migrations/0005_add_career_deep_dive.sql` — **also needs a manual run**). Rendered as a new chapter in `pathwayPages.tsx`, positioned right after "Their natural direction," gated to `tier === "premium"` the same way remedies is. `pricing.ts`'s premium `features` and the FAQ's tier-comparison answer both now name it explicitly — the $15 upgrade's own copy in `ReportView.tsx` was rewritten from "Add gentle remedies" to "Add the deeper chapters," naming both new additions instead of just remedies, directly answering the founder's "$15 gets almost nothing" complaint. The post-upgrade landing chapter (`report/[id]/page.tsx`'s `initialPageId` logic) was updated to land on `career-deep-dive` instead of `remedies`, since career-deep-dive now comes first in page order — landing there means both newly-unlocked chapters are reachable by paging forward, not just the second one.

**A real bug found via verification, fixed before it shipped:** `direction.ts` deliberately lists "Architecture" under *both* the `stem` and `arts` streams (a genuine STEM/arts crossover) — but the naive first pass of `buildCareerDeepDive` flat-mapped every stream's fields without deduplicating, so a field listed in two streams got scored and ranked twice, appearing as a byte-identical duplicate entry in the top-6 list on any chart where it ranked highly (confirmed on 2 of 5 test charts in the first run). Fixed by deduplicating on `fieldName` (first stream's essence framing wins) before scoring; re-verified across 60 random charts with zero duplicates.

**Verification:** `npm run build`/`npm run lint` clean throughout. `buildCareerDeepDive` verified directly against multiple random charts (including the duplicate-field bug above, caught and fixed). The gift-toggle UI was verified via the same disposable dev-preview-route convention used throughout this project (§9) — rendered `ReportView` with `tier: null` to reach the pricing chapter, confirmed the toggle and its hidden-input wiring render correctly, then deleted the route. The DB-backed paths (`gift_vouchers` CRUD, `/redeem/[code]` against a real code, the webhook's voucher branch) could **not** be exercised end-to-end in this sandbox — Supabase network access is blocked here (§3's sandbox-quirk note) — so `/redeem/[code]` on an unset-up local Supabase correctly threw the expected "Supabase is not configured" error rather than something unexpected; this is an environment limitation, not evidence the logic is untested, but it does mean **the founder should do one real end-to-end test (a real gift-voucher purchase → redemption, and a real gift-delivery purchase) against the live/preview environment before trusting this in front of customers**, the same way Stripe/Resend features earlier in this project were founder-verified live before being marked done.

**What's not done:** no code change was needed for the founder's `.env` — every piece reuses existing `STRIPE_*`/`RESEND_*`/`SUPABASE_*` env vars, nothing new to configure. Two migrations (`0004`, `0005`) need a manual run against production Supabase before this ships — flagging this explicitly and clearly, the same lesson §10's incident already taught this project once.

---

## 28. Preview webhook signature mismatch, round two — the real root cause this time (1 Sep 2026, fixed, confirmed live)

**Status: root-caused and fixed, confirmed working with a real Sandbox purchase (purchase email received).** §20 point 3 already documented one instance of "Preview webhook signature mismatch" and its fix (a stale `STRIPE_WEBHOOK_SECRET`). This session hit the *same symptom* again while testing the §27 gifting/purchase-email work on Sandbox, but the root cause turned out to be different and more fundamental — worth documenting precisely so it isn't re-chased as if it were §20 again.

**Symptom:** Stripe Sandbox webhook deliveries to `checkout.session.completed` failing with "No signatures found matching the expected signature for payload," even after the founder regenerated the Stripe signing secret, pasted it word-for-word into Vercel's Preview-scoped `STRIPE_WEBHOOK_SECRET`, and redeployed the exact correct commit (confirmed via commit hash in the Deployments tab).

**Red herrings ruled out, in order (useful if this class of bug resurfaces a third time):**
1. *Stale Vercel deployment* (the §20-point-1 gotcha — "Redeploy" on an old deployment row rebuilds that old commit) — checked directly via commit hash, ruled out.
2. *Secret copy-paste error* — ruled out by a word-for-word diff between Stripe's dashboard and Vercel's env var.
3. Temporary diagnostic logging was added to `route.ts` (`[webhook-diagnostic]`, logging only a safe fingerprint of the secret — prefix/length/whitespace, never the full value) to compare against Stripe's dashboard directly. It never printed at all, which was the actual clue, not a dead end.

**Actual root cause: the registered Stripe Sandbox endpoint URL was wrong, not the secret.** It was set to `https://astro-education.vercel.app/api/stripe/webhook` — Vercel's bare `<project>.vercel.app` domain, which **always aliases to the Production deployment**, never to a Preview deployment of a feature branch. Every webhook delivery was actually hitting Production — running whatever was last deployed to the production branch, reading *Production's* `STRIPE_WEBHOOK_SECRET` (a different value from the one being carefully re-verified in Preview's env vars, which is why matching it made no difference). This also explains why the diagnostic log line never appeared: it only existed on this feature branch, which was never deployed to Production.

**Second layer, found once the endpoint URL was corrected:** pointing the endpoint at the Preview deployment's own URL (via Vercel's automatic per-branch URL, `<project>-git-<branch>-<scope>.vercel.app`) surfaced a *different* 401, `"Protected deployment"` (`vercel_auth_enabled: true`) — Vercel's Deployment Protection gates Preview deployments behind Vercel SSO by default, which an unauthenticated Stripe POST can't pass. Fixed via Vercel's **Protection Bypass for Automation**: Project Settings → Deployment Protection → enable it, copy the generated secret, and append `?x-vercel-protection-bypass=<secret>` to the Sandbox endpoint URL in Stripe. Deployment Protection is left on for everything else — only requests carrying that query param get through.

**Fix, concretely:**
- Stripe Sandbox endpoint (`energetic-finesse`) now points at `https://astro-education-git-<branch>-<scope>.vercel.app/api/stripe/webhook?x-vercel-protection-bypass=<secret>` instead of the bare production-alias domain.
- The bare `astro-education.vercel.app` domain (Production alias) still needs its **own**, separately-registered live-mode webhook endpoint in Stripe — untouched by this fix, not re-verified this session (out of scope; production Stripe was independently confirmed live and working per §8 item 4/§20).
- Temporary `[webhook-diagnostic]` logging removed from `route.ts` now that root cause is confirmed (same "add, use, remove" convention as §20's `deb868a`/`027551d`) — the permanent `Sentry.captureException(err)` on a signature failure stays, since that's a genuine improvement independent of this specific bug.

**Architectural lesson for future work on this project (and anywhere webhooks + Vercel Preview meet):** a Preview deployment's *stable* alias is never the bare `<project>.vercel.app` domain — that always tracks Production regardless of which branch you most recently deployed. Testing any inbound webhook (Stripe or otherwise) against a non-production branch requires either Vercel's automatic per-branch URL or a custom domain explicitly assigned to that branch, **and** a Deployment-Protection bypass if Vercel Authentication is on (which it is by default) — a plain preview URL alone will 401 before your code ever runs.

**Verification:** confirmed by the founder with a real Sandbox test purchase after both fixes — webhook delivered successfully, purchase-confirmation email (§27 item 1) received.

**What's not done:** the corrected Sandbox endpoint currently points at this session's specific feature-branch URL (`claude/handoff-context-verify-xjd179`), which stops working once that branch is deleted/merged. The founder has for now kept this as the sandbox branch domain; if ongoing Sandbox testing against a moving target is wanted longer-term, re-pointing the endpoint at either a dedicated long-lived `staging`/`develop` branch (with a real custom subdomain, e.g. `staging.littlestargazer.com`, assigned to it) or re-registering it fresh each time a new feature branch needs testing is worth deciding explicitly rather than leaving it silently stale next time.

---

## 29. Two more founder-caught gaps in §27's gifting/purchase-email work, both fixed (2 Sep 2026)

**Status: both fixed, confirmed live.** Found by the founder actually using the shipped §27 features end-to-end on Preview, right after §28's webhook-delivery fix unblocked real testing.

**1. `/report`'s free intake form had no path to the gift-voucher flow.** §27 item 2's paywall toggle already linked to `/gift` for a buyer without the recipient's birth details ("Don't have their birth details yet? Send a voucher instead") — but that link only appears *after* birth details are entered, at checkout. The free intake form, where someone without those details actually gets stuck first, had nothing pointing them anywhere except the pre-existing `isGift` checkbox (cosmetic framing only, unrelated to this). Fixed in `ReportFlow.tsx`: the same "Don't have their birth details yet? Send a gift voucher instead" line now sits directly under that checkbox too, linking to `/gift`. Verified visually via the usual disposable dev-preview + Playwright convention.

**2. The purchase-confirmation email (§27 item 1) silently never sent, on every plain (non-gift) purchase.** Root cause was two compounding things:
   - The `career_deep_dive` column migration (`0005_add_career_deep_dive.sql`, §27 item 5) had never actually been run against the Supabase database backing this Preview/Sandbox environment — exactly the risk §27's own "what's not done" note flagged and this project has been bitten by once before (§10's incident, the original reason this "migrations don't auto-apply, must be run manually" lesson exists at all). With the column missing, `getReport()`'s `select` failed on every call.
   - `getReport()` (`src/lib/reports/store.ts`) treated *any* Supabase error identically to "no report with this id" — returning `null` with no logging, no exception, nothing. The webhook's email-sending code treats a null `getReport()` as "nothing to do, skip silently" (correct behavior for an actually-missing report, wrong for a broken query) — so the failure was completely invisible, in Sentry or anywhere else, which is exactly why this took several rounds of live testing to pin down instead of showing up immediately.

   Fixed on both sides: the founder ran the migration against the correct Supabase project, and `getReport()` now reports a genuine Supabase error to Sentry before returning `null` (commit `8e73499`), so this exact failure mode — some future missing column or query error on the payment path — won't be silent again. Confirmed by the founder: a fresh plain purchase now delivers the reading-confirmation email correctly.

**Also raised, not a code issue:** the founder separately noted never receiving *Stripe's own* native payment-receipt email on Sandbox purchases — that's a Stripe Dashboard setting (Settings → Business → Emails → "Successful payments"), independent of and unrelated to this app's own `sendReadingEmail`/`sendGiftReadingEmail` templates. Not investigated further this session; worth the founder checking that toggle directly if they want Stripe's receipt email too, on top of (not instead of) this app's own purchase email.

**Lesson worth repeating for future migrations on this project:** a migration file existing in the repo and a migration having actually been run against the database in question are two different facts, and this project has now lost debugging time to the gap between them more than once (§10, and this entry). Any future PR description or handoff note that says "run migration X manually" should be treated as a blocking checklist item to *verify*, not just a note to acknowledge — ideally by checking the actual column/table exists, not by trusting that it was done.

**Correction, confirmed with the founder right after this was first written:** there is only one Supabase database total (see the header note at the top of this document) — Preview and Production were never separate databases to begin with. So "the migration wasn't run in production" wasn't quite the right framing; it's simply "the migration hadn't been run yet, anywhere," and once it was run once (during this session's debugging), it was already live everywhere, production included. No second run against a separate production database was needed.

---

## 30. §26-§29 merged into production (2 Sep 2026)

**Status: done, pushed to `claude/vedic-horoscope-learning-site-fb6fta`, live.** Everything that had accumulated on the working branch (`claude/handoff-task-review-umvc5i` → `claude/handoff-context-verify-xjd179`) since §25's earlier merge is now on production: §26's persona-conversion-test fixes, §27's full gifting overhaul (reading-delivery email, gift-at-paywall delivery, the gift-voucher subsystem, Stripe Promotion Codes, the Career Deep-Dive premium chapter), §28's webhook-routing/Deployment-Protection root-cause and fix, and §29's two founder-caught gaps (the missing `/report`→`/gift` link, and the silently-failing purchase email).

- Merge was **not** conflict-free this time (unlike §25's) — `HANDOFF.md`'s own header and handoff-note sections had diverged on both branches (each described a different "what's merged, what's not" state, since both were true at different times). Resolved by hand, consolidating into a single current header rather than picking one side. Every other file merged cleanly with zero conflicts — the two branches never touched the same application code.
- Both DB migrations this batch needed (`0004_add_gift_vouchers.sql`, `0005_add_career_deep_dive.sql`) were confirmed already applied — see §29's correction above on why "run it in production separately" turned out not to apply here.
- `npm run build` and `npm run lint` both verified clean against the actual merged tree (not just the pre-merge branch) before pushing.
- The Stripe Sandbox webhook endpoint fixed in §28 still points at the now-deleted-or-about-to-be-deleted working branch's specific Preview URL (`claude/handoff-context-verify-xjd179`'s branch domain) — that will stop working once the branch is cleaned up. Re-pointing it at a stable long-lived target (§28's own "what's not done" note) is still an open decision for whoever does Sandbox testing next.

**What's still genuinely open, not done this session:**
1. Test data cleanup — several test report rows and at least one test gift-voucher sit in the live database from this session's own debugging (see the header note). Not cleaned up; a quick manual pass in Supabase's Table Editor is enough.
2. Stripe's own native payment-receipt email toggle (Settings → Business → Emails) was never checked/enabled — separate from this app's own purchase-confirmation email, which does work now.
3. The §23/§24-called-for persona-conversion-test re-run against production, specifically covering the newer gifting/Career-Deep-Dive features, hasn't happened yet — §26 covered the *previous* batch (§21's career/direct-answer work), not this one.
4. The Sandbox webhook endpoint domain question immediately above.

---

## 31. UI/UX redesign — "Botanical Ink" — on a working branch, not yet merged (4 Sep 2026)

**Status: done, pushed to `claude/astro-education-ui-changes-vvbela`, three commits, build+lint clean. Not merged to production.** This is a **presentation-only** redesign — no product content, copy, pricing, chart-calculation logic, or business logic changed anywhere. Every heading and paragraph on the landing page is the same text that was live before; every other page is untouched content-wise. Started from direct founder feedback that the site "looks very AI generated" (named `astrowain.com` and a same-named competitor, `littlestargazers.org`, as examples of the generic-template look to avoid) and closed out with the founder handing off for the night and asking for the redesign built through to the real site, autonomously, with a written summary to review on waking — this section is that summary, kept for continuity the way every other HANDOFF entry is.

**Sequence this session actually followed, worth knowing if picking this up mid-thread:**
1. **Diagnosis** — read the live `page.tsx`/`layout.tsx`/`globals.css`, named the specific tells (Geist, the literal default Next.js font; the pill-badge→headline→rounded-full-CTA→stat-row hero formula repeated identically in 5 sections; icon-in-soft-circle cards; zero real imagery).
2. **Research** — a sourced design-research brief (audience psychographics, Co-Star/CHANI/The Pattern/Wonderbly/Calm design-language comps, why AI-templated pages measurably underperform) published as a Claude Artifact, not saved in-repo. Six recommendations came out of it; the founder agreed to all six.
3. **Design canvas** — 5 rounds of iteration on a multi-artboard Claude Design canvas (also an Artifact, not in-repo), converging through founder feedback: 3 initial layout directions → founder flagged the palette itself as a generic-AI-default cluster (cream+serif+gold-or-purple is as recognizable a tell as the layout was) → a palette/imagery/motion refinement round → founder picked "Warm Observatory" layout + "Botanical Ink" palette, lightened from the original dark mockup for more color rhythm → added working orbit/growth animations (with one real bug caught and fixed: a `stroke-dasharray` sized for the wrong coordinate scale that looked static instead of animating; fixed by switching to the same proven transform+opacity technique already used elsewhere, not by re-guessing the numbers) → removed a constellation-linework motif the founder felt was now redundant → added a real, founder-supplied Pixabay-licensed photo and a nested planet/moon orbit system.
4. **Real build** — everything below.

**What shipped, concretely:**

- **`src/app/globals.css`** — every existing CSS custom-property token (`--primary`, `--accent`, `--background`, `--surface`, `--border`, etc.) was remapped from the old ivory/indigo/gold values to a new **forest ink / rust-paprika / parchment** palette ("Botanical Ink"), deliberately *not* renamed — every `bg-primary`, `text-accent-bright`, `border-border-soft` Tailwind class already used across ~25 files picked up the new palette automatically, without those files needing individual edits. Also added the sitewide motion vocabulary as reusable `.motion-*` CSS classes (see below), all guarded by a `prefers-reduced-motion` block that sets the *resting visible* state rather than just stripping `animation` (which would otherwise leave elements stuck at their hidden starting opacity — a real trap with this drop-in/pop-in style of animation).
- **Fonts** (`src/app/layout.tsx`): Geist + Fraunces → **Work Sans + Petrona**. Note this is a deliberate adaptation from what the founder actually approved on the design canvas (Spectral + Marcellus) — Marcellus ships only a single 400 weight, and the real codebase has dozens of existing `font-serif font-semibold`/`font-bold` headings that would have silently rendered wrong (no visual bold, since the requested weight doesn't exist) with a single-weight font. Petrona is the same "distinctive serif, not Fraunces" family used in the canvas's Direction 1 exploration and carries real 400-700 weights + italics, so every existing heading keeps working. Flagging this clearly in case the founder specifically wants Marcellus's exact letterforms — that would mean auditing and stripping `font-semibold` from every serif heading first.
- **Three new reusable components** (`src/components/`): `RotatingPlanet.tsx` (a small standalone planet spinning on its own axis — explicitly *not* orbiting anything, per the founder's own distinction — sprinkled into page margins on About/FAQ/Gift/the footer for sitewide motion consistency), `GrowingMark.tsx` (the existing `GrowthPathIcon` brand mark, animated: stem draws, then both branches, then the star, looping every 6s), `OrbitField.tsx` (the landing hero's backdrop — chart rings/axes that materialize-and-fade on loop plus a small planet system continuously orbiting, one planet carrying a tilted ring and its own moon — replaces the old `ConstellationSky.tsx`, deleted, no longer referenced anywhere).
- **Landing page** (`src/app/page.tsx`) — fully rebuilt structurally: light-parchment asymmetric hero (`OrbitField` + `GrowingMark`, left-aligned headline replacing the old centered dark-gradient hero), How It Works moved to a dark-forest band with a numbered horizontal layout instead of three identical icon-in-circle cards, and a real photo added to the existing "Why we built this" section (`src/assets/why-we-built-this.jpg`, a Pixabay-licensed sunset silhouette the founder supplied directly, duotone-treated via CSS `filter`+blend to sit inside the new palette). **Every word of copy is unchanged** — same hero headline, same "Our tone" strength/growth examples, same credibility stats, same "why we built this" paragraphs, same final-CTA text.
- **KundliChart.tsx** — this is the "animated horoscope generation" the founder specifically asked for: the real 12-house grid now drops into place on mount (staggered by grid position), then each real planet placement (and the ascendant badge) pops in on top of the settled grid, in the order the calculation conceptually happens — houses, then planets. Pure CSS animation (no client JS, stays a server component), so it fires automatically every time a real chart renders — `/sample`, the free preview, and every paid reading — not a separate loading screen bolted on.
- **Sitewide geometry pass** — every `rounded-full` CTA button and `rounded-full`/pill badge across the whole site (nav, footer, every page's CTA, every form's submit button, `BookReader`'s Next/Previous, `ReportView`'s TierCard unlock buttons) moved to a restrained `rounded-sm`; every `rounded-2xl`/`rounded-3xl` card (the repeated strength/growth/nurture/subject cards in `ReportView.tsx` and `pathwayPages.tsx`, blog post cards, form cards) moved to `rounded-md`/`rounded-lg`. Four intentional exceptions kept at a larger `rounded-2xl` on purpose — the main book-reader frame, the gift/redeem form's outer card, and the TierCard's own outer card — since those are single primary containers, not the repeated small-card pattern that was the actual AI-slop tell. **One real mistake caught before it shipped**: an early attempt at this via a chained shell command silently no-op'd partway through (a `grep -l | xargs sed` in a `&&` chain died silently when an earlier stage found zero matches, so later stages never ran) — this was caught by re-grepping and screenshotting rather than trusting the first pass, and re-run correctly per-file afterward.

**Verified, not just written:** `npm run build` and `npm run lint` both clean. Visually verified via a local `next dev` + a temporary Playwright install (per this project's own §9 convention — installed, used, uninstalled before the final commit, never shipped) across desktop and mobile viewports: landing page (hero, how-it-works, sample-tone, credibility, why-we-built-this with the real photo, final CTA), `/about`, `/faq`, `/blog`, `/gift`, and a full click-through of `/sample`'s book reader including the animated chart reveal settling correctly with real chart data. One near-miss worth recording: a first full-page screenshot made the "How it works" and "Our tone" sections look completely empty — this turned out to be a `framer-motion` `whileInView` scroll-reveal never firing because the screenshot tool resized the viewport instead of actually scrolling, not a real bug; re-screenshotting with genuine incremental scroll events confirmed every section renders correctly. Worth remembering if a future screenshot of this site ever looks like content is missing — check whether the page was actually scrolled through first.

**What a future agent (or the founder) should still do before this goes to production:**
1. **Human review of the actual live branch preview** — nothing here was approved page-by-page the way the hero direction itself was (3+ rounds of founder iteration on a design canvas); the propagation to About/FAQ/Blog/report-reading-UI/forms was executed and screenshot-checked by the agent alone under an explicit "run it overnight" instruction, not reviewed live by the founder yet.
2. **Font decision** — confirm Petrona (not Marcellus) is acceptable, per the note above.
3. **Photo licensing** — the founder said this was "grabbed from free pictures listed on Pixabay." Pixabay's content license is genuinely free for commercial use without attribution, so this should be fine, but neither the founder nor this session independently confirmed the exact source URL/terms for *this specific image* — worth a 30-second double-check before high-traffic launch, same diligence this project already applies to third-party services (§10, §11).
4. **Merge decision** — same pattern as every other feature batch in this document (§25, §30): don't merge silently. Tell the founder concretely what's in the commits before pushing to `claude/vedic-horoscope-learning-site-fb6fta`.
5. Re-run the §7/§18-style persona conversion test after this ships, same as every prior visual/content change in this project's history — a redesign this size is exactly the kind of change that test exists to catch regressions on.

**Round 2 (same day), from founder feedback on the actual live branch — four fixes, all done, one more commit:**
1. `GrowingMark` in the hero was overlapping `OrbitField`'s outer ring and getting clipped by the section edge (a real placement bug, not a rendering issue) — moved into the open space under the hero copy.
2. `KundliChart`'s house/planet reveal (§31 above) was too fast to actually watch — every stagger and keyframe duration roughly tripled; the full reveal now takes ~4s instead of ~1.5s.
3. **New `AmbientPlanet.tsx`** — the founder specifically wanted something continuously moving in the background on every page, not just the small per-page `RotatingPlanet` accents (which apparently went unnoticed, likely because they're easy to scroll past). This is `fixed`-positioned (not per-section), so it stays in the corner of the viewport regardless of scroll or which page you're on — a ringed planet, low-opacity, rotating and slowly drifting. Mounted once in `layout.tsx`, not per-page.
4. **New `UnlockReveal.tsx`** — the founder's sharpest catch: after a real purchase, the report just rendered the unlocked content with zero acknowledgment anything had changed. This wraps `ReportView`'s return and, only on the exact request that just wrote a fresh unlock (`justUnlockedTier`, already tracked server-side in `report/[id]/page.tsx` for the read-after-write race fix in §20 — reused here, not duplicated), shows a ~3s "Opening {child}'s full reading..." moment that casts their real chart once more before fading away to reveal the reading underneath. Every other page load (including revisiting an already-unlocked report) costs nothing — `active` is false and the overlay never renders.

Verified via the same disposable dev-preview-route convention (§9) — a `justUnlocked`-forced render of `ReportView` with sample data — since exercising this for real means a real Stripe purchase against the one shared database (§3, header note). Route deleted before committing. Build+lint clean; the `.next` cache needed clearing once after deleting the route, same stale-type-validator artifact §26 already documented.

**Round 3 (same day) — three more fixes from a founder screenshot review, all real bugs, not taste calls:**
1. `GrowingMark`'s star used `fill="currentColor"`, so wrapping the mark in `text-primary` (green, for the stem/branches) made the *whole* mark green — the two-tone green-stem/rust-star look from the design canvas was lost the moment it hit real code. Fixed by hardcoding the star to `fill="var(--accent)"` directly so it stays rust regardless of what color context wraps the mark. Worth remembering if `GrowingMark` is ever reused somewhere with a different wrapping color.
2. `AmbientPlanet` (round 2, above) had a **negative** z-index, so every opaque section background on every page painted over it — it was only ever visible in the narrow case where a page's background color happened to match the body's. That's exactly why it read as "keeps disappearing" while scrolling, not a one-off glitch. Fixed with a positive z-index (above normal content, still below the header/`UnlockReveal`) and moved from a bottom corner that bled off-screen to a fully on-screen, vertically-centered position. Also caught in the same pass: with the z-index fixed, it now visibly stacked on top of the *homepage's own* `OrbitField` in the same corner, looking like a mistake rather than richness — `AmbientPlanet` now checks `usePathname()` and renders nothing on `/`, since that hero already has its own dedicated planet system.
3. `OrbitField`'s hero backdrop is a 1440-wide viewBox with `preserveAspectRatio="slice"`, centered right-of-middle — on an actual mobile viewport that crops to a near-meaningless sliver of one ring's arc bleeding in from the right edge, confirmed by screenshot (this is the "cuts the orbit" the founder flagged, and a real regression from the old site, which had no such issue). Hidden below the `sm` breakpoint, matching how `GrowingMark` and every other decorative motif on this page already degrades on mobile, rather than shipping a visibly broken crop.

All three verified by screenshot (desktop hero, About page top+bottom-of-scroll, mobile hero) before committing. Build+lint clean.

**Round 4 (same day) — twinkling-star glow-up, then three attempts to fix one star landing on the `/report` name field (the third one was the real fix):**

1. The scattered twinkle stars added in Round 3-era work (six `AmbientPlanet` sparkle marks) started as tiny 2-3px opacity-pulsing dots and were, per founder feedback, "too tiny even to spot" and read as dots rather than stars. Replaced with real four-point sparkle SVG shapes (14-22px) reusing the existing `SparkleIcon` path family, and widened the `twinkle` keyframe (`globals.css`) to swing opacity 0.12→1 and scale 0.35→1.3 with a `filter: drop-shadow(0 0 7px currentColor)` glow at peak, so they visibly glow up big and fade back down rather than subtly flicker.
2. Founder then reported one specific star sparkling directly over the `/report` intake form's "Child's first name" field. **First attempt**: moved that star from `left:88%` to `left:47%` based on a desktop (1440px) screenshot. Verified clean at 1440px — but this only checked one width, not the actual failure condition.
3. Founder sent a real mobile screenshot: the star was still on the field. Assumed a viewport-width edge case and widened `AmbientPlanet`'s hide breakpoint from `sm:block` (640px) to `md:block` (768px), Playwright-verified hidden through 767px / visible at 768px+. **This was still wrong** — it treated the symptom (some narrow-ish width shows the overlap) without identifying why.
4. Founder confirmed it still happened on mobile. Rather than guess a third breakpoint number, asked directly what device/mode was in use. Answer: "I am in desktop site on my Samsung flip 7 phone" — Chrome's "Request Desktop Site" forces a desktop-width CSS viewport (commonly ~980px) regardless of the phone's real screen size, and that's a fundamentally different failure mode than "narrow mobile viewport." Checking `ReportFlow.tsx` (the actual page content, not the decorative layer) showed its single-column-to-two-column layout switch happens at Tailwind's `lg` breakpoint (1024px) — a full 256px gap (768-1023px) where `AmbientPlanet` was already showing (`md:block`) while the form was still single-column and nearly full-width (measured: the name field's bounding box at 980px spanned 94% of the viewport width). Any star placed anywhere across that width would eventually sit over the field at some point in that gap — the bug was never about which star or its exact `left%`, it was that the decorative layer's own breakpoint didn't match the content layout's breakpoint. **Real fix**: changed `AmbientPlanet`'s wrapper to `lg:block`, matching `ReportFlow.tsx` exactly. Playwright-verified `display: none` at 768/980/1023px and `block` at 1024/1440px; screenshotted `/report` at 980px (the founder's exact scenario) confirmed clean, and `/about` at 1440px confirmed the ambient layer still displays normally on genuine desktop widths.

Lesson for whoever picks up decorative/ambient layers on this site: when a fixed-position overlay coexists with responsive content, its show/hide breakpoint must match the breakpoint the actual content layout switches on — not an arbitrary Tailwind step chosen by what "looks like desktop." A device rendering at an unusual CSS viewport width (desktop-site mode on a phone, a resized browser window, etc.) will always find a mismatch if one exists.

Build+lint clean; verified via Playwright (uninstalled before commit, per §9) at 768/980/1023/1024/1440px. Pushed as a fourth commit on `claude/astro-education-ui-changes-vvbela`.
